# SmartPrintAI Unified TODO

Last sync: 2026-03-16
Sources: PRD.md, MARKETING.md, CLAUDE.md, production work completed on VPS.

## Done (Completed)
- [x] VPS foundation up (Postgres, Redis, MinIO)
- [x] Reverse proxy + HTTPS live (Nginx + Let's Encrypt)
- [x] Domain DNS pointed and production reachable at https://smartprintai.com
- [x] Production app service running via systemd (smartprintai)
- [x] Printful catalog sync flow implemented and active products cleaned
- [x] Product listing route fixed and live
- [x] API validation hardening across routes
- [x] Stripe webhook idempotency and webhook authenticity verification
- [x] Stripe webhook shipping-address fallback + manual-review recovery safety patch (2026-03-24: `/api/webhooks/stripe` now falls back to `customer_details.address` when `shipping_details.address` is absent, added regression coverage for this exact payload shape, shipped one-time idempotent recovery utility `npm run orders:recover:manual-review` for paid `manual_review` orders with missing shipping payload, hardened script env bootstrap on VPS (`.env` + `.env.local` load order), enabled retry eligibility for previously failed recoveries that still carry the manual-review marker and no fulfillment ID, added missing Gooten `BillingAddress` + `Payment` fields in both webhook and recovery handoff payloads, switched Printful order creation to `confirm=1` plus `external_id` upsert mode (`update_existing=1`) to prevent duplicate drafts on retries, kept Printful `stitch_color` auto-retry fallback in `createOrder`, and upgraded post-purchase timeline/CTA trust UX + brand colors on `/success`)
- [x] Post-fix order-pipeline hardening verification (2026-03-24: completed VPS audit for recovered live order state + Stripe payment state; added checkout phone capture (`phone_number_collection`) and propagated customer phone into Printful/Gooten fulfillment payloads (webhook + manual-review recovery utility) to avoid Gooten manual approval due missing recipient phone; added Stripe webhook duplicate replay regression test to assert idempotent `P2002` handling skips provider/email side effects)
- [x] Structured API logging + request IDs
- [x] Rate limiting in place on auth/generation-sensitive paths
- [x] Test stack in place (unit + API + e2e smoke) and CI check script
- [x] Deployment/runbook baseline documented
- [x] Vercel cost-control defaults added (`vercel.json` with `npm ci --prefer-offline --no-audit` + Git-aware `ignoreCommand` to skip no-change builds)
- [x] One-command VPS deployment script added (`npm run deploy:vps`) with sync + build + restart + health checks (2026-03-11)
- [x] Auth magic-link flow implemented
- [x] Account order-history page implemented
- [x] Email delivery setup completed (Resend sending + ImprovMX forwarding)
- [x] Auth redirect bug fixed (/api/auth/verify no longer redirects to localhost)

## Pending (Priority Order)

### P0 - Launch Blocking
- [x] Complete 5 real end-to-end test orders (PRD Week 3-4)
- [x] Test order #1 validated in test mode (checkout -> signed webhook -> order status `processing` with `printfulOrderId`)
- [x] Test order #2 validated in test mode (checkout -> signed webhook -> order status `processing` with `printfulOrderId`)
- [x] Test order #3 validated in test mode (checkout -> signed webhook -> order status `processing` with `printfulOrderId`)
- [x] Test order #4 validated in test mode (checkout -> signed webhook -> order status `processing` with `printfulOrderId`)
- [x] Test order #5 validated in test mode (checkout -> signed webhook -> order status `processing` with `printfulOrderId`)
- [x] Validate full order lifecycle in production (paid -> created -> fulfilled -> shipped)
- [x] Configure `PRINTFUL_WEBHOOK_SECRET` in production and verify signed shipment webhooks
- [x] Enable Stripe live mode and verify live webhook flow
- [x] Rotate all exposed/leaked keys before launch (Resend + Printful rotated and validated on VPS on 2026-03-11; Printful auth `200`, Resend sending endpoint `200`)
- [x] Final production secret audit (.env.local) (2026-03-11 masked check on VPS: required keys present, suspicious bare-token lines none)
- [ ] Run one real live purchase on production and confirm payment settlement end-to-end (deferred by request until core implementation is complete)
- [x] Add production live-purchase preflight automation (2026-03-13: shipped `npm run prod:live-preflight` + checklist `docs/PROD_LIVE_PURCHASE_CHECKLIST.md` to validate all non-owner prerequisites before the real payment run)
- [x] Re-run production live-purchase preflight before owner checkout (2026-03-20: executed `npm run prod:live-preflight` against `https://smartprintai.com`; all endpoint checks passed (`/`, `/create`, `/products`, `/blog`, `/api/products`, `/google/merchant-feed.xml`), non-existent `session_id` returned expected `404`, and live checkout URL generation via `/api/checkout` succeeded)
- [x] Refresh owner live-checkout handoff package (2026-03-20: reran `npm run prod:live-preflight` after Wave 9.1 GREEN validation and rate-limit tuning deployment (`8924b89`), reconfirmed endpoint health + `/api/checkout` readiness, and captured a fresh live Stripe session `cs_live_a1tmfHgxFj4YwaukuuUiYBfzs0PbmD7XV6vU5dctuPoqGHCa81WF6lc7po` with owner action log in `docs/reports/LIVE_PURCHASE_HANDOFF_2026-03-20.md`)
- [x] Masked secret audit run on 2026-03-09 (Stripe currently in test mode; `PRINTFUL_WEBHOOK_SECRET` subsequently configured and verified)
- [x] Stripe live keys applied on VPS and verified (`checkout_http=200`, signed webhook `200`)
- [x] Fixed production key-rotation script bug that wrote blank keys (hidden prompt newlines no longer pollute captured values) (2026-03-11)

### P1 - Product and Revenue Readiness
- [x] Enable AI-mockup eligibility for Gelato products (2026-03-15)
- [x] Confirm prompt -> generation -> mockup -> checkout flow with production-grade QA pass (2026-03-11: VPS sanity chain `generate=200`, `mockup=200`, `checkout=200`)
- [x] Gemini paid quota/billing enabled and validated by live generation request (`/api/generate` returning 200 on 2026-03-11)
- [x] Verify generated design image URLs open publicly after storage proxy/policy fix (2026-03-11 automated live check: `/api/generate` `200`, returned `imageUrl`, direct `HEAD` to image `200`)
- [x] Verify `/products`, product detail, and `/create` product picker can use real product thumbnails (2026-03-11 live URL audit: product images checked `19`, color previews checked `44`, broken `0`)
- [x] Upgrade homepage visual merchandising with real featured-product thumbnails and curated 8-card sample design gallery linked to `/create` prompts (2026-03-11)
- [x] Premium homepage redesign (2026-03-13: homepage-only premium light/dark treatment, editorial hero/layout pass, upgraded nav/footer styling, and verification via `npm run lint`, `npm run test`, and `npm run build` with local placeholder secrets + local service URLs)
- [x] Homepage scroll-story art alignment (2026-03-13: `How It Works` now uses only curated gallery artwork previews, removed the duplicate text overlay from the sticky preview stage, and smoothed the scroll-driven image transitions; verified with `npm run lint`, `npm run test`, and `npm run build`)
- [x] Homepage premium theme smoothing (2026-03-13: removed forced root dark-mode pinning, aligned homepage `color-scheme` behavior with system light/dark, and restyled the homepage discount popup to match the premium landing palette; verified with `npm run lint`, `npm run test`, and `npm run build`)
- [x] Homepage manual theme override (2026-03-13: added homepage `Auto` / `Light` / `Dark` switcher to override browser preference for the landing page and keep premium palette control visible while preserving system mode as default; verified with `npm run lint`, `npm run test`, and `npm run build`)
- [x] Global site theme persistence (2026-03-13: promoted theme choice to a site-wide preference via root `html` theme classes, added persistent theme initialization + shared toggle logic, and exposed the selector in global navigation so chosen mode carries across homepage and non-home pages; verified with `npm run lint`, `npm run test`, and `npm run build`)
- [x] Global theme control cleanup + light-surface parity for app shell, products, and sign-in (2026-03-13: removed the duplicate homepage theme control, reduced selection to nav-only `Light` / `Dark`, migrated legacy `system` storage handling, and refreshed non-home surfaces so `/products` and `/signin` visibly follow the persisted theme; verified with `npm run lint`, `npm run test`, and `npm run build`)
- [x] Global theme variable selector fix (2026-03-13: corrected root CSS variable scoping so `theme-light` no longer inherits dark tokens, restoring real visual light-mode switching on non-home pages; verified with `npm run lint`, `npm run test`, and `npm run build`)
- [x] Global dark-mode selector hardening across non-home routes (2026-03-13: switched root theme CSS selectors to `html[data-theme-mode='light'|'dark']` so production CSS reliably ships both token sets and dark mode applies on `/create`, `/products`, `/signin`, and other app pages; verified with `npm run lint`, `npm run test`, and `npm run build` using local placeholder envs)
- [x] Homepage redesign refinement (2026-03-13: unified top-aligned section headers, scroll-reveal motion on landing sections, and removed visible careers discovery from nav/footer/sitemap)
- [x] Homepage cinematic scroll pass (2026-03-13: added Framer Motion reveal timing, immersive sticky "How It Works" story cards with centered scroll choreography, and smoother premium theme surface transitions)
- [x] Homepage scroll-story repair (2026-03-13: replaced the broken overlapping sticky card stack with a cleaner centered image stage, supporting side previews, and a readable active-step footer)
- [x] Homepage scroll-story cleanup (2026-03-13: removed the oversized blank-scroll gap and simplified desktop "How It Works" to one sticky premium preview image with smoother step-driven transitions)
- [x] Brand identity V2 + premium dark landing direction implementation (2026-03-22: replaced homepage with a single-file premium dark-mode-first landing concept in `src/components/home/HomeLanding.tsx` (hero, trust stack, how-it-works, featured products, differentiators, FAQ, final CTA), introduced reusable brand icon component `src/components/brand/BrandMark.tsx`, aligned nav/footer logo usage to the new mark, and normalized premium-home shell styling to dark-first behavior in `src/app/globals.css`; documented strategy/logo/UI system in `docs/brand/SMARTPRINTAI_BRAND_SYSTEM_V2.md`; added regression coverage in `src/app/home-brand-v2-regression.test.ts`; verified with `npm run test -- src/app/home-brand-v2-regression.test.ts`, `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Homepage conversion optimization + micro-UX tightening pass (2026-03-22: sharpened hero copy and CTA strategy, added reassurance chips and support/terms trust framing near top CTA, re-ordered landing narrative for faster value understanding (`why` before `how`), added mid-funnel conversion CTA block, tightened mobile-first spacing/stacking in key sections, enriched featured-product clarity with use-case microcopy, and made the `Start Creating` navbar CTA visible on mobile; expanded homepage regression assertions in `src/app/home-brand-v2-regression.test.ts`; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Brand identity alignment pass for homepage/layout + icons (2026-03-22: unified homepage/layout accent usage to the finalized blue->teal SmartPrintAI gradient, upgraded nav/footer lockups and `BrandMark` styling, migrated app/metadata icons (`icon`, `apple-icon`, `favicon.ico`, `opengraph-image`, `twitter-image`) off legacy purple motif, updated manifest theme/background colors, and synchronized brand-system palette docs to the final identity; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Support page CTA brand alignment fix (2026-03-22: replaced support form submit button gradient from legacy purple/pink to primary SmartPrintAI blue->teal CTA styling in `src/components/support/SupportPageClient.tsx`, matching navbar/home primary action behavior while preserving existing layout and submit logic; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Homepage conversion instrumentation + funnel guardrails (2026-03-22: extended `src/lib/analytics.ts` with stable homepage/create event taxonomy + helper wrappers, added client-side `src/components/home/HomeFunnelAnalytics.tsx` for homepage pageview/CTA/section/scroll instrumentation with once-per-view section and scroll dedupe, wired `data-home-cta` and `data-home-section` attributes across homepage + navbar/footer CTAs, and tracked `create_flow_started` in `src/components/create/CreatePageClient.tsx` using referrer entrypoint classification; documented event map in `docs/HOMEPAGE_ANALYTICS_FUNNEL.md`; updated regression/analytics tests in `src/app/home-brand-v2-regression.test.ts` and `src/lib/analytics.test.ts`; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Homepage funnel reporting layer + first measured optimization iteration (2026-03-22: added `/api/analytics/events` sink to persist validated homepage funnel events into `data/analytics/homepage-events.jsonl`, introduced `src/lib/homepage-funnel-report.ts` aggregation utilities (core funnel, CTA/device/page-variant breakdowns, biggest drop-off detection, simulated fallback when no log exists), added `npm run analytics:funnel:report` (`scripts/report_homepage_funnel.ts`) to print readable metrics + write `docs/reports/artifacts/homepage-funnel/latest.json`, and expanded tests (`src/lib/homepage-funnel-report.test.ts`, `src/app/api/analytics/events/route.test.ts`, analytics/homepage regressions); applied one optimization based on report output by making mobile navbar create CTA explicit (`Create`) in `src/components/layout/Navbar.tsx`; verified with `npm run lint`, `npm run test`, `npm run analytics:funnel:report`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Real event-log funnel reporting + hero/nav top-funnel conversion tightening (2026-03-22: removed simulated-fallback dependency so homepage funnel reporting is `event_log`-first with zero-data handling (`recordCount`, `hasData`), kept persistence through `/api/analytics/events` and decoupled homepage event forwarding from GA availability (`trackEvent` now forwards funnel events even if `window.gtag` is unavailable), validated end-to-end route persistence with real sample events, and tightened hero/navbar conversion copy (`Create My Product` intent with clearer value proposition + reassurance) without redesigning lower-page sections; updated docs and regression/unit coverage (`src/lib/homepage-funnel-report.test.ts`, `src/lib/analytics.test.ts`, `src/app/home-brand-v2-regression.test.ts`); verified with `npm run lint`, `npm run test`, `npm run analytics:funnel:report`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Homepage hero A/B experimentation framework + variant funnel segmentation (2026-03-22: added deterministic homepage hero variant assignment in `middleware.ts` using stable visitor/variant cookies (`spai_visitor_id`, `spai_home_hero_variant`) and SSR request header handoff (`x-spai-home-hero-variant`) to avoid flicker; extended `HomeLanding` with control `variant_a` + experimental `variant_b` copy for hero headline/subheadline/primary CTA/reassurance; propagated `page_variant` across homepage and create-start events (`trackCreateFlowStarted`) and updated funnel aggregation/report output to compare variant CTR/create-start/drop-off; updated analytics/docs/regression coverage (`src/lib/homepage-experiment.test.ts`, `src/lib/homepage-funnel-report.test.ts`, `src/lib/analytics.test.ts`, `src/app/home-brand-v2-regression.test.ts`, `src/app/api/analytics/events/route.test.ts`); verified with `npm run lint`, `npm run test`, `npm run analytics:funnel:report`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Hero experiment decision guardrails + actionable readout states (2026-03-22: extended `src/lib/homepage-funnel-report.ts` with explicit A/B decision layer (`insufficient_data`, `ready_for_comparison`, `winner_candidate`, `inconclusive`) and recommendations (`continue_running`, `investigate_tracking`, `ship_winner`, `iterate_loser_dimension`) driven by explicit thresholds (`200` experiment-eligible homepage views total, `75` per variant, `10` clicks per variant); made primary/secondary metric definitions explicit (`homepage_to_create_ctr`, `create_start_rate`), added comparison deltas and winner candidate output, and upgraded report CLI readability (`scripts/report_homepage_funnel.ts`) for non-engineer consumption; expanded guardrail coverage in `src/lib/homepage-funnel-report.test.ts` (small-sample, ready, winner, inconclusive, tracking anomaly cases) and documented interpretation rules in `docs/HOMEPAGE_ANALYTICS_FUNNEL.md`; verified with `npm run lint`, `npm run test`, `npm run analytics:funnel:report`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Hero experiment ops monitoring snapshots + readiness progress command (2026-03-22: added local snapshot pipeline (`npm run analytics:hero:snapshot`) that appends daily hero experiment snapshots to `data/analytics/homepage-hero-experiment-snapshots.jsonl` and emits a readiness transition alert when the experiment first becomes comparison-eligible; added concise readiness command (`npm run analytics:hero:readiness`) and extended funnel report output with threshold progress + exact remaining counts per variant/click metric; expanded report model in `src/lib/homepage-funnel-report.ts` with structured readiness progress items/blockers/readiness message; added monitoring helper module + tests (`src/lib/hero-experiment-monitoring.ts`, `src/lib/hero-experiment-monitoring.test.ts`) plus readiness/progress coverage in `src/lib/homepage-funnel-report.test.ts`; documented operations commands/storage/interpretation in `docs/HOMEPAGE_ANALYTICS_FUNNEL.md`; verified with `npm run lint`, `npm run test`, `npm run analytics:funnel:report`, `npm run analytics:hero:readiness`, `npm run analytics:hero:snapshot`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Create-flow entry funnel instrumentation + first-screen clarity pass (2026-03-22: expanded analytics taxonomy with create-entry events (`create_page_viewed`, `create_entrypoint_resolved`, `create_prompt_input_focused`, `create_prompt_started`, `create_generation_started`, `create_template_selected`, `create_product_selected`, `create_flow_abandoned_early`) and persisted them through `/api/analytics/events`; instrumented `/create` entry interactions in `CreatePageClient` + `PromptInput` with first-fire dedupe and early-abandon inference; added create-entry aggregation/reporting (`src/lib/create-entry-funnel-report.ts`, `npm run analytics:create:entry-report`, artifact `docs/reports/artifacts/create-entry-funnel/latest.json`) including first-friction drop-off readout; applied one focused UX clarification at the top of `/create` via explicit step-1 guidance copy block + clearer localized subtitle sequencing; updated docs/tests (`docs/HOMEPAGE_ANALYTICS_FUNNEL.md`, analytics/api/report tests); verified with `npm run lint`, `npm run test`, `npm run analytics:create:entry-report`, `npm run analytics:funnel:report`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Create-entry decision guardrails + readiness snapshot monitoring (2026-03-22: extended `src/lib/create-entry-funnel-report.ts` with explicit decision model (`status`, `decision`, `reason`, `nextAction`), default readiness thresholds (`minCreatePageViewed=120`, `minPromptInputFocused=70`, `minPromptStarted=45`, `minGenerationStarted=25`), primary/secondary metrics (`prompt_start_rate_from_create_view`, `generation_start_rate_from_prompt_start`), threshold progress blockers, and actionable friction gating (`firstActionableFrictionPoint` only when status is `friction_candidate`); added operational commands `npm run analytics:create:entry-readiness` and `npm run analytics:create:entry-snapshot` with snapshot persistence to `data/analytics/create-entry-funnel-snapshots.jsonl` (`CREATE_ENTRY_FUNNEL_SNAPSHOT_PATH` override) plus readiness transition alerting; added monitoring helper/tests (`src/lib/create-entry-monitoring.ts`, `src/lib/create-entry-monitoring.test.ts`) and expanded create-entry report tests for threshold/decision edge cases; updated docs in `docs/HOMEPAGE_ANALYTICS_FUNNEL.md` with exact iteration rule; verified with `npm run lint`, `npm run test`, `npm run analytics:create:entry-report`, `npm run analytics:create:entry-readiness`, `npm run analytics:create:entry-snapshot`, `npm run analytics:funnel:report`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Homepage product-proof conversion section + instrumentation (2026-03-22: added a new premium `product_proof` section in `src/components/home/HomeLanding.tsx` positioned after trust to demonstrate prompt -> generated design -> applied product outcome in <5s, with direct `/create` CTA (`product_proof_primary_create`) and localized copy; added dedicated analytics events (`product_proof_section_viewed`, `product_proof_cta_clicked`) in `src/lib/analytics.ts` and wired trigger logic in `src/components/home/HomeFunnelAnalytics.tsx` while preserving existing hero experiment behavior; extended validation coverage in `src/app/home-brand-v2-regression.test.ts`, `src/lib/analytics.test.ts`, `src/lib/homepage-funnel-report.test.ts`, and `src/app/api/analytics/events/route.test.ts`; updated event taxonomy docs in `docs/HOMEPAGE_ANALYTICS_FUNNEL.md`; verified with `npm run lint`, `npm run test`, `npm run analytics:funnel:report`, `npm run analytics:create:entry-report`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Product-proof exposure impact reporting (2026-03-22: added visitor-linked exposure segmentation to homepage funnel reporting in `src/lib/homepage-funnel-report.ts` to compare `product_proof_exposed` vs `product_proof_not_exposed` cohorts (CTR, create-start rate, click->create-start rate, cohort deltas, interpretation state); wired visitor linkage by forwarding `visitor_id` from `spai_visitor_id` cookie in homepage tracker (`src/components/home/HomeFunnelAnalytics.tsx`) and create-start instrumentation (`src/components/create/CreatePageClient.tsx`); extended report CLI output (`scripts/report_homepage_funnel.ts`) with cohort tables/deltas/interpretation and added regression coverage for exposure segmentation and low-data edge cases (`src/lib/homepage-funnel-report.test.ts`); updated analytics docs with exposure methodology in `docs/HOMEPAGE_ANALYTICS_FUNNEL.md`; verified with `npm run lint`, `npm run test`, `npm run analytics:funnel:report`, `npm run analytics:create:entry-report`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Analytics visitor-id integrity hardening across homepage/create funnel events (2026-03-22: enforced `visitor_id` injection in `trackEvent` (`src/lib/analytics.ts`) with cookie reuse + fallback generation/persistence so every forwarded funnel event is visitor-linked; hardened `/api/analytics/events` (`src/app/api/analytics/events/route.ts`) to validate/repair missing IDs server-side (payload -> cookie -> generated), flag repaired payloads in logs, and set `spai_visitor_id` when absent; expanded route/client analytics regression coverage in `src/app/api/analytics/events/route.test.ts` and `src/lib/analytics.test.ts`; updated analytics integrity notes in `docs/HOMEPAGE_ANALYTICS_FUNNEL.md`; validated with `npm run lint`, `npm run test`, `npm run analytics:funnel:report`, `npm run analytics:create:entry-report`, and env-backed `npm run build`)
- [x] Traffic source attribution across homepage/create funnel analytics (2026-03-22: added first-touch attribution model in `src/lib/analytics-attribution.ts` (`utm_*`, `referrer`, `referrer_domain`, `landing_path`, `device_type`) with normalized `direct`/`internal`/`unknown` buckets and cookie persistence (`spai_first_touch_attribution`), auto-enriched all forwarded funnel events in `src/lib/analytics.ts`, and backfilled/validated attribution server-side in `/api/analytics/events` (`src/app/api/analytics/events/route.ts`); extended homepage and create-entry reporting with segmentation by `utm_source`, `utm_campaign`, `referrer_domain`, and `device_type` plus hero-variant-by-source matrix (`src/lib/homepage-funnel-report.ts`, `src/lib/create-entry-funnel-report.ts`, `scripts/report_homepage_funnel.ts`, `scripts/report_create_entry_funnel.ts`); expanded analytics/report regression coverage (`src/lib/analytics.test.ts`, `src/app/api/analytics/events/route.test.ts`, `src/lib/homepage-funnel-report.test.ts`, `src/lib/create-entry-funnel-report.test.ts`) and updated attribution docs in `docs/HOMEPAGE_ANALYTICS_FUNNEL.md`; validated with `npm run lint`, `npm run test`, `npm run analytics:funnel:report`, `npm run analytics:create:entry-report`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Attribution QA pass + canonical UTM naming contract for traffic launch readiness (2026-03-22: added canonical source/medium normalization guardrails in `src/lib/analytics-attribution.ts` (aliases such as `facebook`/`instagram` -> `meta`, `twitter` -> `x`, `cpc`/`ppc` -> `paid_search`, plus referrer-domain fallback mapping), added controlled tagged-traffic QA command `npm run analytics:attribution:qa` via `scripts/qa_attribution_launch_readiness.ts` to validate homepage->create attribution persistence and bucket integrity, extended report scripts with fallback-coverage warnings in `scripts/report_homepage_funnel.ts` and `scripts/report_create_entry_funnel.ts`, expanded attribution/API regression coverage in `src/lib/analytics-attribution.test.ts` and `src/app/api/analytics/events/route.test.ts`, and documented the launch contract + checklist in `docs/HOMEPAGE_ANALYTICS_FUNNEL.md`; validated with `npm run lint`, `npm run test`, `npm run analytics:attribution:qa`, `npm run analytics:funnel:report`, `npm run analytics:create:entry-report`, and env-backed `npm run build`)
- [x] Traffic launch packet for controlled first acquisition batch (2026-03-22: added `docs/marketing/TRAFFIC_LAUNCH_PACKET.md` with copy-paste canonical tagged URLs per platform (`meta`, `google`, `tiktok`, `x`), 48h click-capped pacing plan, measurement window/thresholds, explicit success and red-flag criteria, required post-launch validation commands, and one-variable-at-a-time decision checkpoints for hero + create-entry optimization readiness)
- [x] Product detail color/size selectors are now interactive and passed into `/create` query params (2026-03-10)
- [x] `/create` now applies selected product/color/size and regenerates mockup when color changes (2026-03-10)
- [x] `/create` upload-and-edit flow (2026-03-13: customers can upload their own PNG/JPG/WEBP reference photo, send it through `/api/generate`, and receive AI-edited print-ready output; verified with `npm run lint`, `npm run test`, and `npm run build` using placeholder local env values)
- [x] `/create` uploaded-photo generation reliability fix (2026-03-13: fixed production `413` payload failures by compressing large reference images client-side before `/api/generate`, added explicit generation error messaging for non-JSON and 413 responses, and aligned API source-image size validation; verified with `npm run lint`, `npm run test`, and `npm run build` using placeholder local env values)
- [x] Uploaded-reference generation fallback hardening (2026-03-13: improved Gemini response parsing across all candidates/parts and added source-image failure fallback to text-only generation when provider cannot process or returns no image, preventing hard `500` dead ends on `/api/generate`; verified with `npm run lint`, `npm run test`, and `npm run build` using placeholder local env values)
- [x] `/create` mockup preview reliability under Printful throttling (2026-03-14: `/api/mockup` now returns explicit `429` retry metadata when Printful throttles, and create-page mockup flow retries with bounded backoff while preserving loading state/error messaging so product preview resolves instead of staying blank; verified with `npm run lint`, `npm run test`, and `npm run build` using placeholder local env values)
- [x] `/create` mockup retry loop guard for long Printful throttle windows (2026-03-14: adjusted client retry timing to wait past provider `retry-after` boundaries (+3s safety buffer), lowered retry attempts, and expanded max retry window so previews stop looping on minute-boundary `429` responses and recover or fail fast with message; verified with `npm run lint`, `npm run test`, and `npm run build` using placeholder local env values)
- [x] `/create` AI-mockup eligibility gate for unsupported catalog products (2026-03-14: excluded Adidas + Printful product `507` from create-page product picker and blocked their `/api/mockup` requests server-side with explicit non-eligible error, so unsupported products stay sellable in `/products` while avoiding broken preview loops in AI flow; verified with `npm run lint`, `npm run test`, and `npm run build` using placeholder local env values)
- [x] `/products` split into AI-customizable vs ready-to-buy catalog sections (2026-03-14: grouped product listing by mockup eligibility, exposed non-eligible items in a dedicated section, and removed AI-design CTA on unsupported product detail pages while keeping them purchasable as standard catalog items; verified with `npm run lint`, `npm run test`, and `npm run build` using placeholder local env values)
- [x] Ready-to-buy product detail purchase CTA restoration + checkout fulfillment safety (2026-03-20: fixed non-AI product detail pages to show explicit `Add to Cart` + `Go to Cart` CTAs (default + localized routes) instead of only the informational banner, localized CTA copy in `productDetail` i18n entries, and hardened Stripe webhook mapping to upsert fallback `Design` records for ready-to-buy synthetic design IDs so order item foreign keys and provider image payloads stay valid; added regression coverage in `src/app/product-detail-ready-buy-regression.test.ts` and `src/app/api/webhooks/stripe/route.test.ts`; verified with `npm run test -- src/app/api/webhooks/stripe/route.test.ts src/app/product-detail-ready-buy-regression.test.ts`, `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Gelato provider integration scaffold + sync script (2026-03-14: added typed `src/lib/gelato.ts` client wrappers for catalog/product/price endpoints, added `scripts/sync_gelato_products.ts` (`npm run sync:products:gelato`) to ingest Gelato catalog SKUs as `gelato:*` ready-to-buy products, and updated mockup-eligibility to restrict AI flow to numeric Printful ids only; verified with `npm run lint`, `npm run test`, and `npm run build` using placeholder local env values)
- [x] Extend GelatoClient with mockup and order creation methods (2026-03-15)
- [x] Support Gelato provider in `/api/mockup` (2026-03-15: detects provider, generates mockup via template creation if Gelato, otherwise uses Printful; verified with `npm run lint`, `npm run test`)
- [x] Gelato template mockup readiness hardening (2026-03-15: `/api/mockup` now resolves template placeholder names from Gelato template payloads and polls created store products until preview media is available; if preview rendering is still pending it returns retryable `429` metadata instead of hard `500`; added extractor coverage in `src/lib/gelato.test.ts`; verified with `npm run lint`, `npm run test`, and `npm run build` with local dummy env overrides)
- [x] Update Stripe webhook for Gelato fulfillment (2026-03-15: detects provider, routes to either `printful.createOrder` or `gelato.createOrder` using variant mapping; verified with `npm run lint`, `npm run test`)
- [x] Gelato fulfillment payload compliance hardening (2026-03-15: `gelato.createOrder` now enforces required `orderReferenceId` and `currency`, Stripe webhook now passes normalized checkout currency to Gelato orders, and added Gelato webhook test coverage for mixed-provider routing safety; verified with `npm run lint`, `npm run test`, and `npm run build` with local dummy env overrides)
- [x] Unified provider product sync automation (2026-03-14: introduced `scripts/sync_products.ts` as the default `npm run sync:products` flow to run Printful sync plus conditional Gelato sync when env is present, then print AI-customizable vs ready-to-buy isolation totals by provider; verified with `npm run lint`, `npm run test`, and `npm run build` using placeholder local env values)
- [x] Gelato sync resilience for unavailable catalog products (2026-03-14: hardened `scripts/sync_gelato_products.ts` to skip per-product 403/404/price-missing provider errors and continue syncing remaining SKUs so unified provider sync no longer aborts on partial Gelato catalog gaps; verified with `npm run lint`, `npm run test`, and `npm run build` using placeholder local env values)
- [x] Gelato price payload compatibility fix (2026-03-14: updated `extractGelatoMinUnitPrice` to parse Gelato array-shaped `/v3/products/{uid}/prices` responses and added regression tests in `src/lib/gelato.test.ts`, so sync can read provider prices when returned as list entries; verified with `npm run lint`, `npm run test`, and `npm run build` using placeholder local env values)
- [x] Gelato production API integration verified (2026-03-14: set `GELATO_API_KEY` + catalog UID list on VPS and ran `npm run sync:products` successfully in production; results `gelato_synced=161`, `skippedUnavailable=32`, isolation totals `active=180`, `aiCustomizable=14`, `readyToBuy=166`)
- [x] Gelato catalog presentation cleanup (2026-03-14: improved Gelato sync metadata extraction with readable attribute-based names, size/color parsing, printable flag tracking, and catalog-level fallback images so cards are no longer blank; split `/products` and localized `/[locale]/products` into `AI Customizable`, `Printable Catalog` (Gelato), and `Ready-to-Buy` sections for clearer catalog isolation; verified with `npm run lint`, `npm run test`, and `npm run build` using placeholder local env values)
- [x] Gelato image-mixing rollback (2026-03-14: removed Printful-image fallback reuse from Gelato sync so catalog cards no longer show repeated/mismatched Printful photos; Gelato products now only persist native Gelato media when provided by API, preventing cross-provider thumbnail pollution; verified with `npm run lint`, `npm run test`, and `npm run build` using placeholder local env values)
- [x] Gelato catalog sync dedupe + stale cleanup hardening (2026-03-14: updated `scripts/sync_gelato_products.ts` to page full catalog results, dedupe repeated variant-name rows per catalog, sanitize non-Gelato image URLs, and deactivate stale `gelato:*` products not present in current sync, eliminating legacy repeated-card carryover after provider sync; verified with `npm run lint`, `npm run test`, and `NODE_ENV=production npm run build` sourced from `.env.example`)
- [x] Gelato store-template sync mode + fixed template mapping scaffold (2026-03-14: added `GELATO_SYNC_MODE=store-templates` path in `scripts/sync_gelato_products.ts` that hydrates Gelato products from store listings and template UID matching, upserts stable `gelato:template:*` rows with provider metadata, and deactivates stale noisy Gelato catalog rows; added `src/lib/gelato-template-mapping.ts` defaults for SmartPrintAI store + 5 template IDs and new ecommerce extractors/tests in `src/lib/gelato.ts`; updated unified sync guard and env docs for `GELATO_ECOMMERCE_BASE_URL`, `GELATO_SYNC_MODE`, `GELATO_STORE_ID`, and `GELATO_TEMPLATE_MAP_JSON`; verified with `npm run lint`, `npm run test`, and `NODE_ENV=production npm run build` sourced from `.env.example`)
- [x] Gelato template mapping dedupe cleanup for duplicate t-shirt entries (2026-03-15: normalized `productType` parsing and deduped `GELATO_TEMPLATE_MAP_JSON` entries by product type so repeated `t-shirt` templates collapse to one active mapping; added regression coverage in `src/lib/gelato-template-mapping.test.ts`; verified with `npm run test -- src/lib/gelato-template-mapping.test.ts`, `npm run lint`, and `npm run build` with local dummy env overrides)
- [x] Gelato store-template sync fallback for post-cleanup template lookup failures (2026-03-15: updated `scripts/sync_gelato_products.ts` to keep syncing by store-product title when `/v1/templates/{id}` lookups are unavailable, and to use store `parentTemplateId` when present while persisting template-validation status in `printArea`; added `extractGelatoStoreProductTemplateId` coverage in `src/lib/gelato.test.ts`; verified with `npm run test -- src/lib/gelato.test.ts`, `npm run lint`, and `DATABASE_URL=postgresql://user:pass@127.0.0.1:5432/smartprintai STRIPE_SECRET_KEY=sk_test_placeholder RESEND_API_KEY=re_test_placeholder npm run build`)
- [x] Gelato AI-eligibility guard for invalid template mappings (2026-03-15: updated `isMockupEligibleProduct` to require a validated Gelato template mapping when `printArea.providerTemplateValidated` is present, wired all catalog/create/mockup/margin summary callers to pass `printArea`, and added regression coverage in `src/lib/mockup-eligibility.test.ts`; verified with `npm run test -- src/lib/mockup-eligibility.test.ts src/lib/gelato.test.ts`, `npm run lint`, and `DATABASE_URL=postgresql://user:pass@127.0.0.1:5432/smartprintai STRIPE_SECRET_KEY=sk_test_placeholder RESEND_API_KEY=re_test_placeholder npm run build`)
- [x] Gelato template ID refresh to latest store templates (2026-03-15: updated default `GELATO_TEMPLATE_MAP` IDs for t-shirt/mug/hoodie/poster/canvas to the newly regenerated template IDs, and refreshed default mapping regression expectation; verified with `npm run test -- src/lib/gelato-template-mapping.test.ts`, `npm run lint`, and `DATABASE_URL=postgresql://user:pass@127.0.0.1:5432/smartprintai STRIPE_SECRET_KEY=sk_test_placeholder RESEND_API_KEY=re_test_placeholder npm run build`)
- [x] Gelato blank-mockup guard for templates without image placeholders (2026-03-15: `scripts/sync_gelato_products.ts` now persists `providerTemplateHasPlaceholders`/`providerTemplatePlaceholderName`, Gelato mockup eligibility requires placeholders when template validation metadata is present, and `/api/mockup` returns an explicit placeholder-missing `400` instead of silently returning blank previews; verified with `npm run test -- src/lib/mockup-eligibility.test.ts src/lib/gelato-template-mapping.test.ts`, `npm run lint`, and `DATABASE_URL=postgresql://user:pass@127.0.0.1:5432/smartprintai STRIPE_SECRET_KEY=sk_test_placeholder RESEND_API_KEY=re_test_placeholder npm run build`)
- [x] Gelato hybrid apparel-catalog sync mode for men/women/unisex expansion (2026-03-15: added `GELATO_SYNC_MODE=hybrid` support to combine store-template sync with catalog ingestion in one run, added optional catalog auto-discovery via `/v3/catalogs`, and added apparel/gender filters (`GELATO_SYNC_APPAREL_ONLY`, `GELATO_SYNC_GARMENT_CUTS`) so large Gelato clothing catalogs can be synced without flooding non-apparel SKUs; updated `.env.example`; verified with `npm run test -- src/lib/gelato.test.ts src/lib/mockup-eligibility.test.ts src/lib/gelato-template-mapping.test.ts`, `npm run lint`, and `DATABASE_URL=postgresql://user:pass@127.0.0.1:5432/smartprintai STRIPE_SECRET_KEY=sk_test_placeholder RESEND_API_KEY=re_test_placeholder npm run build`)
- [x] Gelato curated-catalog filters for premium starter assortment sizing (2026-03-16: added optional sync controls `GELATO_SYNC_INCLUDE_KEYWORDS`, `GELATO_SYNC_EXCLUDE_KEYWORDS`, `GELATO_SYNC_MIN_BASE_PRICE`, and `GELATO_SYNC_REQUIRE_IMAGE` so imports can target ~30-50 higher-value products and skip blank-image/low-price rows; updated `scripts/sync_gelato_products.ts` and `.env.example`; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@127.0.0.1:5432/smartprintai STRIPE_SECRET_KEY=sk_test_placeholder RESEND_API_KEY=re_test_placeholder npm run build`)
- [x] Catalog blank-image cleanup + core color curation for product browsing (2026-03-17: filtered listing/API outputs to hide products without image URLs, added shared core-color curation (`Black/White/Blue/Grey`) and applied it in Printful/Gelato/Gooten sync scripts, added Gelato/Gooten color-preview extraction for variant mappings, and limited product-detail color selectors to the curated core set; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db RESEND_API_KEY=dummy STRIPE_SECRET_KEY=sk_test_dummy npm run build`)
- [x] Gooten listing color-parity fix in `/api/products` (2026-03-17: curated `gooten:*` product colors to the same 4 core options (`Black/White/Blue/Grey`) in API responses so `/create` and product-list consumers no longer surface long raw provider color lists; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=re_dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Product detail color-visibility fallback when provider color previews are missing (2026-03-17: improved detail-page color swatch hex resolution by color name and added a subtle selected-color image tint overlay fallback whenever a color has no provider-specific preview image, so color selection remains visually reflected for Gooten rows that ship without variant media URLs; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db RESEND_API_KEY=dummy STRIPE_SECRET_KEY=sk_test_dummy npm run build`)
- [x] Product detail fallback recoloring for provider datasets without per-color preview media (2026-03-17: upgraded fallback rendering in `ProductDetailClient` from background-only tint to image-level recolor filters (with white/black handling) so Gooten color selection produces visible product-color changes even when provider does not supply color-specific preview URLs; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=re_dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Gooten detail preview artifact rollback for missing per-color media (2026-03-17: removed aggressive fallback hue/sepia recolor filters in `ProductDetailClient` that were producing purple/black visual artifacts, kept stable base product image rendering when provider-specific color media is unavailable, and added explicit preview-note messaging with selected-color context so customers are not misled; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=re_dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Gooten blank-image source mapping hardening + unreliable media suppression (2026-03-18: hardened `extractGootenProductImageUrl`/variant preview mapping in `src/lib/gooten.ts` to normalize malformed nested URLs (for example duplicated `https://.../https://...` feed values), require reliable storefront media hosts for synced catalog imagery (`appassets.azureedge.net` + `*.cdn.gooten.com`), and suppress unsupported media-host products from sync by treating them as missing image media; added regression coverage in `src/lib/gooten.test.ts`; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Gooten `/create` mockup rendering hotfix for S3 preview host mismatch (2026-03-19: traced P1 incident to live Gooten preview URLs resolving to `https://s3.amazonaws.com/gooten-imgmanip/...` (verified via VPS runtime DB samples and direct provider preview responses) while Next image allowlist omitted that host; added `next.config.mjs` remote pattern for `s3.amazonaws.com` + `/gooten-imgmanip/**` and regression coverage in `src/app/security-headers-regression.test.ts` to prevent host allowlist regression; verified with `npm run lint`, `npm run test -- src/app/security-headers-regression.test.ts`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Gooten `/create` mockup readiness guard for transient S3 `403` previews (2026-03-19: reproduced failing `gooten:244` colors where `/api/mockup` returned `200` with `mockupUrl` but raw S3 preview URL returned `403 application/xml` for ~4 seconds before becoming `200 image/png`, causing browser broken-image state; added remote preview readiness utility (`src/lib/remote-image.ts`) with regression coverage (`src/lib/remote-image.test.ts`), hardened `/api/mockup` to validate cached Gooten preview URLs before reuse, evict unavailable cache entries, and block fresh Gooten responses until remote image HEAD check is ready (otherwise return retryable `429` with `retry-after`); verified with `npm run lint`, `npm run test -- src/lib/remote-image.test.ts src/app/security-headers-regression.test.ts`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Gooten provider integration scaffold + curated sync gate (2026-03-16: added typed `src/lib/gooten.ts` API client + payload extractors with tests, added `scripts/sync_gooten_products.ts` (`npm run sync:products:gooten`) for curated Gooten catalog ingestion as `gooten:*` products, extended `src/lib/product-provider.ts` and `scripts/sync_products.ts` so unified sync can optionally include Gooten when `GOOTEN_SYNC_ENABLED=1`, and documented required env vars in `.env.example`; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres STRIPE_SECRET_KEY=sk_test_dummy RESEND_API_KEY=re_dummy npm run build`)
- [x] Gooten AI-mockup readiness via SKU mapping + preview API (2026-03-16: added variant lookup/mapping extraction in `src/lib/gooten.ts`, enriched `scripts/sync_gooten_products.ts` to persist `providerDefaultSku` + `variantMapping` + country/currency metadata from `/productvariants` lookups, enabled `/api/mockup` Gooten branch to generate previews through `/productpreview`, and updated mockup eligibility so only mapped `gooten:*` products enter AI flow; updated `.env.example` with Gooten country/currency knobs and added regression tests in `src/lib/gooten.test.ts` + `src/lib/mockup-eligibility.test.ts`; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres STRIPE_SECRET_KEY=sk_test_dummy RESEND_API_KEY=re_dummy npm run build`)
- [x] Stripe webhook fulfillment routing for Gooten orders (2026-03-16: updated `/api/webhooks/stripe` to route `gooten:*` items through `getGootenClient().createOrder()` using SKU mapping (`size:color` -> color -> size -> default SKU), preserved mixed-provider behavior, and persisted Gooten order IDs in existing order tracking fields; added webhook regression coverage for Gooten order creation path in `src/app/api/webhooks/stripe/route.test.ts`; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres STRIPE_SECRET_KEY=sk_test_dummy RESEND_API_KEY=re_dummy npm run build`)
- [x] Gooten sync endpoint compatibility fallback (2026-03-16: updated `src/lib/gooten.ts` so `listProducts()` falls back from legacy `/v/201608/products` failures (`405/500`) to Gooten catalog feed (`catalog.json`), added catalog-tree flattening + normalized product/image/category/price parsing for feed payloads, and expanded extractor regression tests in `src/lib/gooten.test.ts`; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres STRIPE_SECRET_KEY=sk_test_dummy RESEND_API_KEY=re_dummy npm run build`)
- [x] Gooten mockup preview `SpaceId` auto-resolution retry (2026-03-16: updated `/api/mockup` Gooten flow to parse provider validation errors for valid `SpaceId` candidates and retry preview generation with discovered IDs, plus added parser coverage in `src/lib/gooten.test.ts`; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/smartprintai STRIPE_SECRET_KEY=sk_test_dummy STRIPE_WEBHOOK_SECRET=whsec_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=re_dummy STORAGE_ENDPOINT=https://example.com STORAGE_REGION=us-east-1 STORAGE_ACCESS_KEY=dummy STORAGE_SECRET_KEY=dummy STORAGE_BUCKET=dummy NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Gooten mockup `SpaceId` payload parity fix for `Images[0]` requirements (2026-03-16: updated `/api/mockup` Gooten retry path to inject resolved `SpaceId`/`spaceId` at both payload root and first image object (`Images[0]` / `images[0]`) before each preview attempt, aligning with provider validation errors that require nested image-level `SpaceId`; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres STRIPE_SECRET_KEY=sk_test_dummy STRIPE_WEBHOOK_SECRET=whsec_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=re_dummy BLOB_READ_WRITE_TOKEN=dummy BLOB_READ_TOKEN=dummy BLOB_WRITE_TOKEN=dummy NEXT_PUBLIC_APP_URL=http://localhost:3000 npm run build`)
- [x] Gooten mockup LayerId auto-resolution for multi-space SKUs (2026-03-16: updated `/api/mockup` Gooten flow to parse `LayerId` valid-option errors, retry previews with resolved `SpaceId + LayerId`, and normalize preview payload images to provider-supported `Image.Url` structure; added `LayerId` extraction regression coverage in `src/lib/gooten.test.ts`; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres STRIPE_SECRET_KEY=sk_test_dummy STRIPE_WEBHOOK_SECRET=whsec_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=re_dummy BLOB_READ_WRITE_TOKEN=dummy BLOB_READ_TOKEN=dummy BLOB_WRITE_TOKEN=dummy NEXT_PUBLIC_APP_URL=http://localhost:3000 npm run build`)
- [x] Gooten opaque `500` preview fallback via sentinel LayerId probe (2026-03-16: updated `/api/mockup` retry loop to force a controlled probe attempt with `layerId=00000` after each unresolved `SpaceId`, so Gooten returns valid `LayerId` options which are then retried automatically for final preview generation; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres STRIPE_SECRET_KEY=sk_test_dummy STRIPE_WEBHOOK_SECRET=whsec_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=re_dummy BLOB_READ_WRITE_TOKEN=dummy BLOB_READ_TOKEN=dummy BLOB_WRITE_TOKEN=dummy NEXT_PUBLIC_APP_URL=http://localhost:3000 npm run build`)
- [x] Production Gelato clothing-catalog sync activation (2026-03-15: configured VPS to hybrid scoped catalogs `apparel,t-shirts,hoodies` with mens/womens/unisex filters, reran `npm run sync:products`, and confirmed live isolation totals `active=30`, `aiCustomizable=14`, `readyToBuy=16`, with Gelato `total=11` now ingested and active)
- [x] Gelato product-description sanitization on detail pages (2026-03-14: added `normalizeProductDescription` to decode entity-encoded markup, strip HTML/table artifacts, and trim noisy provider text before rendering SEO metadata, JSON-LD, and product detail copy in both localized and default routes; verified with `npm run lint`, `npm run test`, and `NODE_ENV=production npm run build` sourced from `.env.example`)
- [x] Build product margin cheat sheet (provider, base cost, sell price, Stripe fee estimate, net margin) for pricing decisions (2026-03-15: added `npm run pricing:margin-cheatsheet` via `scripts/build_margin_cheatsheet.ts` to generate `docs/pricing/MARGIN_CHEAT_SHEET.csv` and `docs/pricing/MARGIN_CHEAT_SHEET.md` from active catalog data, grouped by provider with top/bottom net-margin highlights; verified with `npm run lint`, `npm run test`, and `npm run build` with local dummy env overrides; runtime requires `DATABASE_URL`)
- [x] Margin cheat sheet earnings expansion (2026-03-20: upgraded `scripts/build_margin_cheatsheet.ts` + new `src/lib/margin-cheatsheet.ts` to compute per-product total revenue, provider shipping cost, AI generation cost, Stripe fees, total cost, and net earnings (including Gooten provider detection), added HTTP catalog fallback source (`MARGIN_PRODUCT_SOURCE_URL` / `NEXT_PUBLIC_APP_URL`) when `DATABASE_URL` is unavailable, and added regression tests in `src/lib/margin-cheatsheet.test.ts`; regenerated `docs/pricing/MARGIN_CHEAT_SHEET.csv` and `docs/pricing/MARGIN_CHEAT_SHEET.md` from live API catalog data; verified with `npm run lint`, `npm run test -- src/lib/margin-cheatsheet.test.ts`, `npm run test`, env-backed `npm run build`, and `npm run pricing:margin-cheatsheet`)
- [x] `/api/mockup` now supports non-`front` Printful placements via product file-type fallback (MG-4 recovery) (2026-03-11)
- [x] Printful sync mapper now stores per-color preview image URLs when provided by Printful variants (2026-03-10)
- [x] Production product sync run to backfill color preview image metadata (2026-03-10)
- [x] Color parity audit completed against live Printful variants (`products_checked=19`, `mismatch_count=0`) on 2026-03-10
- [x] Product color-availability audit completed against live Printful catalog (2026-03-11): no missing non-default colors in app; 7 products are genuinely single-color (`White`) in Printful, and 5 home/accessory products use `Default` with no explicit variant color field from Printful
- [x] Manual visual QA on live site completed via automated browser run (desktop+mobile, 7 multi-color products, `failureCount=0`) on 2026-03-11
- [x] Confirm production order confirmation + post-order customer communication sequence (order confirmation + shipment notification templates verified in inbox)
- [x] Add/verify tracking email trigger on shipped status (`shipment_sent`/`package_shipped` webhook -> shipped status transition + shipment email, idempotent)
- [x] Add baseline customer support flow (new `/support` page + `/api/support` handling; support and contact routing + SLA auto-reply)

### P1 - Analytics and SEO Baseline (from PRD + Marketing)
- [x] GA4 installed with purchase conversion tracking (`NEXT_PUBLIC_GA_MEASUREMENT_ID` set on production VPS on 2026-03-09)
- [x] GA4 code instrumentation deployed (sitewide gtag + purchase event on success page); pending only `NEXT_PUBLIC_GA_MEASUREMENT_ID` env value in production
- [x] Google Search Console setup + sitemap submission (domain property verified via DNS TXT and `https://smartprintai.com/sitemap.xml` accepted on 2026-03-09)
- [x] Google Merchant feed compliance patch shipped (2026-03-12): fixed category mapping to real synced values (`apparel/accessories/home/drinkware`), normalized default color to `White`, and added feed attributes for Google review (`g:product_type`, `g:color`, `g:gender`, `g:age_group`, `g:size`) for all feed items to accelerate Merchant issue resolution
- [x] Google Merchant Center warnings cleared on live catalog (2026-03-12 manual verification: `Attention requise` empty; all 19 products in review/green pipeline)
- [x] Google Ads account linked to Merchant Center with billing profile attached (2026-03-12; service visible under `Accès et services`)
- [x] SEO optimization pass for product pages (titles, metadata, canonical tags, Product/ItemList schema, crawl controls)
- [x] Wave 1 semantic guarantee for `/create` and localized `/[locale]/create` H1 fallback (2026-03-17: upgraded Suspense fallback in both routes to render server-side hero heading copy so crawlers and no-JS fallbacks always see an H1; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=re_dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 1 canonical/hreflang deduplication for localized routes (2026-03-17: introduced `buildLocaleCanonical()` and switched all localized `generateMetadata` canonical values to locale-aware helper paths so default `en` canonicals resolve to unprefixed URLs while retaining `fr/de/es` localized canonicals and shared hreflang alternates; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=re_dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 1 HTTP security header baseline (2026-03-17: added global headers in `next.config.mjs` for CSP (`frame-ancestors 'none'` + Stripe allowances), HSTS, `X-Frame-Options`, `X-Content-Type-Options`, and strict-origin referrer policy while preserving checkout/generation routes; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=re_dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 1 trust/legal visibility pass (2026-03-17: added `/about`, `/privacy`, `/terms` pages with metadata, surfaced legal/support links in global footer and support link in navbar, and added trust/legal URLs to sitemap for crawl + discoverability; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=re_dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 1 image delivery optimization for storefront media (2026-03-17: enabled AVIF/WebP response formats and image caching in `next.config.mjs`, added live provider domains (`files.cdn.printful.com`, `az412349.cdn.gooten.com`, `appassets.azureedge.net`) to Next image allowlist, and removed `unoptimized` from home/product storefront render paths to restore responsive optimization without touching `/create` generation flow; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=dummy NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 1 internal-linking pass for blog/category discovery pages (2026-03-18: added contextual cross-links between `/blog` and `/products`/`/create` on default and localized blog indexes, and added product-discovery links from default/localized `/products` pages back to `/blog` and `/create` to strengthen crawl paths and reduce weak isolated pages; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=dummy NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 2 localized metadata completion for social previews (2026-03-18: added locale-aware OpenGraph/Twitter metadata generation for home/create/products list/product detail/blog list/blog detail/support templates across default and localized routes, aligned localized `og:url` with canonical policy (including `en` collapse to unprefixed paths), and added regression coverage in `src/lib/metadata.test.ts` + `src/app/metadata-regression.test.ts`; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 2 CSP hardening stage 2 (safe) (2026-03-18: removed `unsafe-eval` from default CSP mode, added `script-src-attr 'none'`, externalized app-owned inline bootstrap scripts to `/theme-init.js` + `/ga4-init.js`, introduced rollback-safe `CSP_MODE=legacy` path, documented compatibility + nonce/hash rollout plan in `docs/CSP_HARDENING_STAGE2.md`, and added `CSP_MODE` to `.env.example`; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 2 structured-data enrichment (2026-03-18: added reusable schema helpers in `src/lib/schema.ts` with regression tests in `src/lib/schema.test.ts`, injected `BreadcrumbList` JSON-LD across default/localized create, products, product-detail, blog-index, and blog-detail templates, and extended product detail `Offer` schema with normalized `priceCurrency`, `availability`, `shippingDetails`, and `hasMerchantReturnPolicy` fields without changing canonical/hreflang behavior; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 2 trust conversion blocks on money pages (2026-03-18: added reusable localized trust strip component (`src/components/shared/TrustSignalStrip.tsx`) backed by locale-aware support/terms link model (`src/lib/trust.ts` + `src/lib/trust.test.ts`) and rendered it on default/localized `/create`, `/products`, and product detail templates with concise delivery SLA, support response-time, and return-policy microcopy + links; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 2 automated SEO quality gates (CI-safe) (2026-03-18: added `npm run test:seo:gates` and integrated it into `scripts/ci_non_interactive.sh`, expanded metadata regression contracts for canonical/hreflang parity + metadata thresholds, added security-header regression coverage for CSP/HSTS/XFO/nosniff/referrer-policy baseline, added static internal-link regression checks on core nav/marketing templates, and documented the threshold contract in `docs/SEO_QUALITY_GATES.md`; verified with `npm run test:seo:gates`, `npm run lint`, `npm run test`, `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run ci:check`)
- [x] Wave 2 validation + evidence pack (2026-03-18: reran full local validation (`lint`, `test:seo:gates`, `test`, `build`, `ci:check`), redeployed commit `24102f1` to VPS (`./scripts/deploy_vps.sh --allow-dirty`) and rechecked production OG/Twitter, CSP hardening, schema markers, and trust-block rendering; published date-stamped validation artifact `docs/reports/WAVE2_VALIDATION_2026-03-18.md` with before/after metrics, command evidence, and residual risk notes)
- [x] Wave 3 hreflang rendered-head assertion fix (P1) (2026-03-18: identified root-cause as validation parser mismatch against Next-rendered `hrefLang` alternates in SSR HTML (not missing alternates), added rendered-head parser utility/tests (`src/lib/rendered-head.ts` + `src/lib/rendered-head.test.ts`), added post-build/local-or-prod rendered assertion harness (`npm run seo:assert:rendered` via `scripts/assert_rendered_alternates.ts`) to enforce canonical/alternate/og:url parity on `/create` locale variants and key localized templates, and wired it into `scripts/ci_non_interactive.sh`; verified with `npm run lint`, `npm run test:seo:gates`, `npm run test`, `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`, `SEO_ASSERT_BASE_URL=https://smartprintai.com npm run seo:assert:rendered`, and `SEO_ASSERT_BASE_URL=https://smartprintai.com DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run ci:check`)
- [x] Wave 3 hard verification harness (prod + local) (2026-03-18: added `npm run seo:verify:rendered` via `scripts/verify_rendered_head_harness.ts` to fetch rendered HTML from a managed local `next start` instance plus production, assert canonical/hreflang (`en`, `fr`, `de`, `es`, `x-default`) and `og:url` consistency on key localized templates, and persist timestamped raw HTML + JSON/markdown evidence under `docs/reports/artifacts/wave3-rendered-head-<timestamp>/` and `docs/reports/WAVE3_RENDERED_HEAD_VERIFY_<timestamp>.md`; verified with `npm run lint`, `npm run test`, `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`, and `npm run seo:verify:rendered`)
- [x] Wave 3 CSP hardening phase 3 (safe rollout) (2026-03-18: tightened hardened/default CSP to `script-src 'self' https:` with explicit `script-src-elem 'self' 'unsafe-inline' https:` + `script-src-attr 'none'` for hydration/JSON-LD compatibility, preserved rollback-safe `CSP_MODE=legacy` behavior, expanded security-header regression tests to validate hardened and legacy directive composition, and documented compatibility/rollback notes in `docs/CSP_HARDENING_STAGE3.md`; verified with `npm run lint`, `npm run test:seo:gates`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 3 Lighthouse budget enforcement (true gates) (2026-03-18: added deterministic Lighthouse gate script `npm run perf:lighthouse:gate` (`scripts/lighthouse_budget_gate.ts`) with route warm-up, two-pass median scoring, stable headless Chrome flags, dynamic product-detail discovery from `/products`, and threshold + trend-regression enforcement using `config/lighthouse-budget.json` and committed baseline `docs/reports/LIGHTHOUSE_BASELINE.json`; integrated gate into `scripts/ci_non_interactive.sh` and documented contracts in `docs/SEO_QUALITY_GATES.md`; verified with `npm run lint`, `npm run test:seo:gates`, `npm run perf:lighthouse:gate`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 3 structured-data consistency pass (2026-03-18: fixed locale schema URL generation conflicts by routing localized ItemList/Article schema URLs through canonical-aware helper `buildLocalizedSchemaUrl()` so `en` schema URLs collapse to unprefixed canonical paths while `fr/de/es` remain locale-prefixed; patched `/[locale]/products`, `/[locale]/blog`, and `/[locale]/blog/[slug]`; added regression coverage in `src/lib/schema.test.ts` + `src/app/metadata-regression.test.ts` to enforce schema/canonical/hreflang alignment; verified with `npm run lint`, `npm run test:seo:gates`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 3 compliance prep foundation (Swiss + US) (2026-03-18: added counsel-ready legal review checklist and page/component inventory map in `docs/LEGAL_REVIEW_CHECKLIST_SWISS_US.md`, covering terms/privacy/support/trust/checkout/email/legal-link surfaces with jurisdiction-specific verification tasks and documented gaps; no legal claims were modified in this step; verified with `npm run lint`)
- [x] Wave 3 validation + evidence pack (2026-03-18: reran full verification with `SEO_ASSERT_BASE_URL=https://smartprintai.com DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run ci:check`, reran rendered HTML harness via `npm run seo:verify:rendered`, captured fresh artifacts (`docs/reports/WAVE3_LIGHTHOUSE_GATE_2026-03-18_14-35-36.md`, `docs/reports/WAVE3_RENDERED_HEAD_VERIFY_2026-03-18_14-40-06.md` + timestamped artifact folders), and published consolidated validation report `docs/reports/WAVE3_VALIDATION_2026-03-18.md` with production HTML/head/schema/CSP spot checks and residual risk notes)
- [x] Wave 3 production rollout verification + deploy health-check resilience (2026-03-18: hardened `scripts/deploy_vps.sh` with retry/backoff for local/public curl checks immediately after service restart, redeployed to VPS via `./scripts/deploy_vps.sh --allow-dirty --skip-install --skip-migrate` with all health checks returning `200`, revalidated rendered-head parity using `SEO_ASSERT_BASE_URL=https://smartprintai.com npm run seo:assert:rendered`, and confirmed phase-3 CSP is live on production (`script-src 'self' https:` + `script-src-elem 'self' 'unsafe-inline' https:` + `script-src-attr 'none'`))
- [x] Wave 4 `/create` trust visibility hardening (P1) (2026-03-18: made trust-strip SSR-visible by rendering `TrustSignalStrip` inside Suspense fallback on both default and localized `/create` routes (`src/app/create/page.tsx`, `src/app/[locale]/create/page.tsx`) while preserving hydrated trust rendering in `CreatePageClient`; added regression coverage in `src/app/create-trust-visibility-regression.test.ts` to enforce fallback + hydrated trust visibility contract; verified with `npm run lint`, `npm run test` (26 files, 125 tests), and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 4 rendered-head + trust verification harness upgrade (2026-03-18: upgraded `scripts/verify_rendered_head_harness.ts` to Wave 4 scope with production-first runtime checks, route matrix coverage for `/create` locale variants + `/products` + discovered product detail + blog routes, trust-strip visibility assertions per route locale, and timestamp+commit-SHA artifact/report naming under `docs/reports/artifacts/wave4-rendered-head-*` + `docs/reports/WAVE4_RENDERED_HEAD_VERIFY_*`; added trust parsing helpers in `src/lib/rendered-head.ts` with regression coverage in `src/lib/rendered-head.test.ts`; verified with `npm run lint`, `npm run test` (26 files, 127 tests), `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`, VPS deploy `./scripts/deploy_vps.sh --allow-dirty --skip-install --skip-migrate`, and `npm run seo:verify:rendered` producing `docs/reports/WAVE4_RENDERED_HEAD_VERIFY_2026-03-18_16-16-08_42392c2.md`)
- [x] Wave 4 deploy script reliability tests (2026-03-18: extracted retry logic to `scripts/lib/deploy_healthcheck.sh`, updated `scripts/deploy_vps.sh` to source reusable health-check helper with actionable missing-file failure path, added deterministic mocked shell tests in `scripts/tests/deploy_healthcheck_test.sh` covering immediate success, flaky recovery, and retry exhaustion paths, and wired `npm run test:deploy:vps` into `scripts/ci_non_interactive.sh`; verified with `bash -n scripts/deploy_vps.sh`, `npm run test:deploy:vps`, `npm run lint`, `npm run test` (26 files, 127 tests), `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`, and `SEO_ASSERT_BASE_URL=https://smartprintai.com DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run ci:check`)
- [x] Wave 4 trend-based quality gates for Lighthouse/rendered-head (2026-03-18: added `scripts/quality_trend_gate.ts` + `npm run perf:lighthouse:trend-gate` to aggregate latest Lighthouse + Wave 4 rendered-head summaries, persist rolling history (`docs/reports/artifacts/wave4-trend-history/*.json`), and fail only on statistically significant regressions (rolling window mean/stddev + absolute drop floor) while preserving hard-fail on rendered verification failures; added deterministic fixture tests in `scripts/tests/quality_trend_gate_test.sh` (`npm run test:quality:trend`), integrated rendered verification + trend unit tests + trend gate into `scripts/ci_non_interactive.sh`, and documented contracts/env toggles in `docs/SEO_QUALITY_GATES.md`; verified with `npm run lint`, `npm run test:quality:trend`, `npm run test`, `npm run perf:lighthouse:gate`, and `npm run perf:lighthouse:trend-gate`)
- [x] Wave 4 schema/trust consistency pass on money pages (2026-03-18: added `src/app/money-pages-schema-trust-regression.test.ts` to enforce trust-strip presence and JSON-LD schema anchors across `/create`, `/products`, `/products/[id]`, and localized variants, including Product/Offer and locale-schema path wiring assertions; wired the suite into `npm run test:seo:gates` and documented the new gate in `docs/SEO_QUALITY_GATES.md`; verified with `npm run lint`, `npm run test:seo:gates`, `npm run test` (27 files, 129 tests), and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 4 reporting + ops visibility pack (2026-03-18: generated fresh production rendered-head+trust evidence (`docs/reports/WAVE4_RENDERED_HEAD_VERIFY_2026-03-18_17-21-11_31747e5.md` + `docs/reports/artifacts/wave4-rendered-head-2026-03-18_17-21-11-31747e5/`), executed deploy retry tests (`npm run test:deploy:vps`), executed Lighthouse and trend gates (`docs/reports/WAVE3_LIGHTHOUSE_GATE_2026-03-18_17-21-15.md`, `docs/reports/WAVE4_TREND_GATE_2026-03-18_17-25-40_31747e5.md`, trend history under `docs/reports/artifacts/wave4-trend-history/`), archived prod header/trust spot-checks, and published consolidated validation report `docs/reports/WAVE4_VALIDATION_2026-03-18.md` including before/after `/create` trust visibility and quality trend snapshot; full validation reruns passed: `npm run lint`, `npm run test:deploy:vps`, `npm run seo:verify:rendered`, `npm run perf:lighthouse:gate`, `npm run perf:lighthouse:trend-gate`, `npm run test:seo:gates`, `npm run test`, and env-backed `npm run build`)
- [x] Wave 5 deterministic Lighthouse product-detail fixture routing (P1) (2026-03-18: replaced gate-critical dynamic product-detail discovery with fixture-first resolution in `scripts/lighthouse_budget_gate.ts` using `config/lighthouse-budget.json` `productDetailFixture.path`, added fallback discovery/fallback-path behavior with actionable hard-fail message when unresolved, and surfaced route-resolution strategy metadata in Lighthouse summary/report outputs; added helper + regression coverage in `src/lib/lighthouse-product-route.ts` and `src/lib/lighthouse-product-route.test.ts`; verified with `npm run lint`, `npm run test` (28 files, 135 tests), `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`, and `npm run perf:lighthouse:gate` producing `docs/reports/artifacts/lighthouse-2026-03-18_18-06-18/summary.json` with `productDetailResolution.strategy=\"fixture\"`)
- [x] Wave 5 trend baseline growth automation (2026-03-18: added scheduled checkpoint pipeline (`scripts/run_quality_checkpoint.sh`) plus npm entries (`ops:quality-checkpoint`, `ops:quality-checkpoint:install-timer`), added VPS systemd timer/service units under `ops/systemd/` with installer `scripts/install_quality_checkpoint_timer.sh`, and documented operations in `docs/ops/QUALITY_CHECKPOINT_AUTOMATION.md`; upgraded `scripts/quality_trend_gate.ts` to emit explicit `pass`/`warmup`/`fail` status, warmup baseline metadata, and entry-retention enforcement via `QUALITY_TREND_HISTORY_RETENTION`; expanded deterministic regression coverage in `scripts/tests/quality_trend_gate_test.sh` (warmup + retention assertions); verified with `bash -n scripts/run_quality_checkpoint.sh scripts/install_quality_checkpoint_timer.sh scripts/tests/quality_trend_gate_test.sh`, `npm run test:quality:trend`, `npm run lint`, `npm run test` (28 files, 135 tests), `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`, and `npm run ops:quality-checkpoint`)
- [x] Wave 5 rendered harness semantic assertions v2 (2026-03-18: upgraded `scripts/verify_rendered_head_harness.ts` to assert trust visibility state, money-page schema shape (`BreadcrumbList`, `ItemList`, `Product` offer fields), and legal/support link integrity (support/terms path + reachability) alongside canonical/hreflang/og:url parity; renamed default artifact/report outputs to `wave5-rendered-semantics-*` + `WAVE5_RENDERED_SEMANTICS_VERIFY_*`; expanded rendered parser coverage in `src/lib/rendered-head.ts`/`src/lib/rendered-head.test.ts` (JSON-LD parsing/type extraction, trust-link integrity checks), fixed locale trust terms links to canonical `/terms` in `src/lib/trust.ts` + tests, and updated trend discovery compatibility in `scripts/quality_trend_gate.ts` (Wave 5 prefix with Wave 4 fallback) and docs (`docs/SEO_QUALITY_GATES.md`); verified with `npm run test:quality:trend`, `npm run lint`, `npm run test` (28 files, 137 tests), `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`, `SEO_VERIFY_INCLUDE_LOCAL=1 SEO_VERIFY_INCLUDE_PROD=0 npm run seo:verify:rendered`, and `npm run perf:lighthouse:trend-gate`)
- [x] Wave 5 deploy reliability test expansion + machine-readable deploy status (2026-03-18: upgraded `scripts/deploy_vps.sh` to emit structured deploy summaries (`DEPLOY_REMOTE_STATUS_JSON` from remote runtime and final `DEPLOY_STATUS_JSON` from local runner) with restart/health-check state, stage, and exit metadata; kept actionable failure message on remote failure path; expanded retry test coverage in `scripts/tests/deploy_healthcheck_test.sh` with network exhaustion case; added deterministic mocked deploy-script integration tests in `scripts/tests/deploy_vps_script_test.sh` for success and remote-failure summary contracts; updated `package.json` `test:deploy:vps` to run both suites; verified with `bash -n scripts/deploy_vps.sh scripts/tests/deploy_healthcheck_test.sh scripts/tests/deploy_vps_script_test.sh`, `npm run test:deploy:vps`, `npm run lint`, `npm run test` (28 files, 137 tests), and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 5 ops dashboard snapshot artifact (Mission Control) (2026-03-18: added `scripts/build_quality_ops_snapshot.ts` to generate compact daily JSON + markdown snapshots with RAG gate flags (rendered, lighthouse, trend, deterministic route, trust/schema, legal links), trend deltas/warmup context, and key score/visibility metrics from checkpoint summaries; integrated snapshot generation into `scripts/run_quality_checkpoint.sh` with `latest-snapshot.json` pointer, retention cleanup for snapshot artifacts/reports, and configurable rendered-target toggles (`QUALITY_CHECKPOINT_INCLUDE_LOCAL`, `QUALITY_CHECKPOINT_INCLUDE_PROD`) for local validation without production deploy coupling; added deterministic regression coverage in `scripts/tests/build_quality_ops_snapshot_test.sh`, npm scripts (`ops:quality-snapshot`, `test:quality:snapshot`), and ops docs updates (`docs/ops/QUALITY_CHECKPOINT_AUTOMATION.md`); verified with `bash -n scripts/run_quality_checkpoint.sh scripts/tests/build_quality_ops_snapshot_test.sh`, `npm run test:quality:snapshot`, `npm run ops:quality-snapshot`, `npm run lint`, `npm run test` (28 files, 137 tests), `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`, and `QUALITY_CHECKPOINT_INCLUDE_LOCAL=1 QUALITY_CHECKPOINT_INCLUDE_PROD=0 npm run ops:quality-checkpoint`)
- [x] Wave 5 CI/release guardrail tightening (2026-03-18: enforced deterministic fixture resolution in `scripts/lighthouse_budget_gate.ts` via `LIGHTHOUSE_REQUIRE_FIXTURE` (default `1`) and surfaced the guard in summary/report output; added release assertion script `scripts/ci_quality_guardrails.ts` + deterministic tests `scripts/tests/ci_quality_guardrails_test.sh` to hard-fail on non-fixture Lighthouse strategy, rendered semantic regressions, and trend `fail` status while preserving warmup pass behavior; wired explicit artifact-path guardrail execution into `scripts/ci_non_interactive.sh`, added npm scripts (`ci:quality:guardrails`, `test:quality:ci-guardrails`), and passed fixture enforcement through checkpoint automation (`QUALITY_CHECKPOINT_REQUIRE_FIXTURE`); documented contracts/toggles in `docs/SEO_QUALITY_GATES.md` and `docs/ops/QUALITY_CHECKPOINT_AUTOMATION.md`; verified with `bash -n scripts/ci_non_interactive.sh scripts/run_quality_checkpoint.sh scripts/tests/ci_quality_guardrails_test.sh`, `npm run test:quality:ci-guardrails`, `npm run test:quality:trend`, `npm run lint`, `npm run test` (28 files, 137 tests), `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`, `SEO_VERIFY_INCLUDE_LOCAL=1 SEO_VERIFY_INCLUDE_PROD=0 npm run seo:verify:rendered`, `LIGHTHOUSE_REQUIRE_FIXTURE=1 npm run perf:lighthouse:gate`, `QUALITY_TREND_LIGHTHOUSE_SUMMARY=docs/reports/artifacts/lighthouse-2026-03-18_20-03-02/summary.json QUALITY_TREND_RENDERED_SUMMARY=docs/reports/artifacts/wave5-rendered-semantics-2026-03-18_20-01-24-0485190/summary.json npm run perf:lighthouse:trend-gate`, and `CI_GUARD_REQUIRE_PROD_TARGET=0 CI_GUARD_LIGHTHOUSE_SUMMARY=docs/reports/artifacts/lighthouse-2026-03-18_20-03-02/summary.json CI_GUARD_RENDERED_SUMMARY=docs/reports/artifacts/wave5-rendered-semantics-2026-03-18_20-01-24-0485190/summary.json CI_GUARD_TREND_SUMMARY=docs/reports/artifacts/wave4-lighthouse-trend-2026-03-18_20-07-52-0485190/summary.json npm run ci:quality:guardrails`)
- [x] Wave 5 validation + evidence pack (2026-03-18: deployed commit `99e8498` to VPS via `./scripts/deploy_vps.sh --allow-dirty --skip-install --skip-migrate` with `DEPLOY_STATUS_JSON.status=success`; revalidated full production-targeted gates (`npm run seo:verify:rendered`, `LIGHTHOUSE_REQUIRE_FIXTURE=1 npm run perf:lighthouse:gate`, explicit-summary `npm run perf:lighthouse:trend-gate`, and prod-target `npm run ci:quality:guardrails`); executed repeated `npm run ops:quality-checkpoint` runs to generate/refresh required Wave 5 artifact sets (`wave5-rendered-semantics-*`, `wave5-lighthouse-deterministic-*`, `wave5-trend-history-*`, checkpoint + snapshot JSON/MD); fixed checkpoint trend-input wiring in `scripts/run_quality_checkpoint.sh` so trend history appends checkpoint-owned summaries (`QUALITY_TREND_LIGHTHOUSE_SUMMARY` / `QUALITY_TREND_RENDERED_SUMMARY`) and confirmed history growth (`lighthouseHistory: 1 -> 2`, `renderedHistory: 2 -> 3`); reran `npm run lint`, `npm run test` (28 files, 137 tests), and env-backed `npm run build`; published consolidated report `docs/reports/WAVE5_VALIDATION_2026-03-18.md`)
- [x] Wave 6 warmup completion automation + ETA (2026-03-18: upgraded `scripts/quality_trend_gate.ts` to persist lifecycle state (`trend_lifecycle_state.json`) and auto-mark `mode` from `warmup` to `active` once baseline depth is sufficient, emit transition metadata (`previousMode`, `justActivated`, `activeSince`), and include explicit warmup ETA (`remainingRuns`, checkpoint cadence via `QUALITY_TREND_CHECKPOINT_INTERVAL_HOURS`, estimated activation timestamp) in summary/report output; expanded deterministic coverage in `scripts/tests/quality_trend_gate_test.sh` for warmup ETA, lifecycle transition, and non-repeating activation behavior; documented new lifecycle/ETA controls in `docs/SEO_QUALITY_GATES.md`; verified with `bash -n scripts/tests/quality_trend_gate_test.sh`, `npm run test:quality:trend`, `npm run lint`, `npm run test` (28 files, 137 tests), and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 6 conversion insight pack (weekly source/page/form-step dropoff + anomaly hints) (2026-03-18: added `scripts/build_conversion_insights.ts` to generate weekly conversion insight artifacts (`summary.json` + markdown) from database windows or fixture mode, including source breakdown, page/form-step funnel dropoff, attribution coverage, and anomaly flags with actionable reason hints; integrated automation stage into `scripts/run_quality_checkpoint.sh` (`conversion` stage + artifact retention) and documented operations/env in `docs/ops/QUALITY_CHECKPOINT_AUTOMATION.md`; added deterministic regression coverage in `scripts/tests/conversion_insights_test.sh` and npm scripts (`ops:conversion-insights`, `test:conversion:insights`); verified with `bash -n scripts/tests/conversion_insights_test.sh scripts/run_quality_checkpoint.sh`, `npm run test:conversion:insights`, `npm run lint`, `npm run test` (28 files, 137 tests), and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 6 ops alert tuning (dedupe/cooldown for non-critical alerts, critical immediate) (2026-03-18: added `scripts/build_ops_alert_summary.ts` to aggregate trend + conversion alert candidates, persist alert state (`docs/reports/artifacts/wave6-alert-state/state.json`), suppress recurring warning alerts during configurable cooldown (`QUALITY_ALERT_COOLDOWN_HOURS`), and always emit critical alerts; integrated `alerts` stage into `scripts/run_quality_checkpoint.sh` with artifact/report retention (`wave6-alerts-*`, `WAVE6_ALERTS_*`), updated ops docs (`docs/ops/QUALITY_CHECKPOINT_AUTOMATION.md`), and added deterministic coverage in `scripts/tests/ops_alert_tuning_test.sh` (`test:ops:alerts`) for warning cooldown suppression and critical immediate bypass behavior; verified with `bash -n scripts/tests/ops_alert_tuning_test.sh scripts/run_quality_checkpoint.sh`, `npm run test:ops:alerts`, `npm run test:conversion:insights`, `npm run test:quality:snapshot`, `npm run lint`, `npm run test` (28 files, 137 tests), and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 6 release confidence card (single mission-control JSON with deploy health + conversion pulse) (2026-03-18: upgraded `scripts/build_quality_ops_snapshot.ts` to emit a Wave 6 release-confidence payload with dedicated card sections for rendered semantics, deterministic Lighthouse gate, trend status, derived deploy health, and conversion pulse; added new gate flags (`conversionPulse`, `deployHealth`), conversion-summary ingestion from checkpoint stage paths, and release-card markdown highlights while preserving backward-compatible snapshot artifact outputs; expanded deterministic regression coverage in `scripts/tests/build_quality_ops_snapshot_test.sh` to enforce conversion-critical red behavior and deploy/conversion flag contracts; refreshed ops documentation wording in `docs/ops/QUALITY_CHECKPOINT_AUTOMATION.md`; verified with `npm run test:quality:snapshot`, `npm run test:ops:alerts`, `npm run test:conversion:insights`, `npm run lint`, `npm run test` (28 files, 137 tests), and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 6 validation + reporting pack (2026-03-18: executed validation command matrix (`npm run test:quality:snapshot`, `npm run test:ops:alerts`, `npm run test:conversion:insights`, `npm run lint`, `npm run test`, env-backed `npm run build`) and executed `npm run ops:quality-checkpoint` (pass after elevated runtime due local sandbox IPC restriction) to refresh production-targeted artifacts (`wave5-rendered-semantics-*`, `wave5-lighthouse-deterministic-*`, `wave5-trend-history-*`, `wave6-conversion-insights-*`, `wave6-alerts-*`, `wave5-checkpoints/snapshot-*`); published consolidated report `docs/reports/WAVE6_VALIDATION_2026-03-18.md` with stage statuses, release confidence card summary, artifact inventory, and warmup/conversion availability notes)
- [x] Wave 7 runtime DB connectivity hardening for conversion pulse (P1) (2026-03-18: upgraded `scripts/build_conversion_insights.ts` with explicit runtime DB precheck (`SELECT 1`) and connectivity state contract (`connected_live`, `degraded_no_db`, `stale_cached`), added deterministic degraded policy control via `CONVERSION_INSIGHTS_DEGRADED_POLICY` (`warn` default, optional `fail` hard-exit), and emitted explicit connectivity reason codes/messages in summary output; expanded deterministic coverage in `scripts/tests/conversion_insights_test.sh` for connected fixture mode, degraded missing-DB mode, and fail-policy non-zero exit behavior; verified with `bash -n scripts/tests/conversion_insights_test.sh`, `npm run test:conversion:insights`, `npm run test:ops:alerts`, `npm run lint`, `npm run test` (28 files, 137 tests), and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 7 conversion pulse reliability layer (cache + freshness TTL + stale markers) (2026-03-18: completed cache-backed fallback in `scripts/build_conversion_insights.ts` so short DB outages can serve `stale_cached` conversion pulse mode from cached summary, added deterministic cache-state reason codes for unavailable mode (`*_no_cache`, `*_cache_invalid`), emitted freshness contract (`ttlSeconds`, `sourceGeneratedAt`, `ageSeconds`, `stale`, `cacheFile`) plus top-level `mode`/`reasonCode` in summary/report, and persisted live connected summaries to cache for future fallback; expanded deterministic shell coverage in `scripts/tests/conversion_insights_test.sh` for unavailable-without-cache and cached-fallback scenarios; verified with `bash -n scripts/tests/conversion_insights_test.sh`, `npm run test:conversion:insights`, `npm run test:ops:alerts`, `npm run test:quality:snapshot`, `npm run lint`, `npm run test` (28 files, 137 tests), and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 7 checkpoint execution resilience (retry/backoff + strict critical gates + actionable exit semantics) (2026-03-18: hardened `scripts/run_quality_checkpoint.sh` with configurable DB-dependent retry/backoff (`QUALITY_CHECKPOINT_DB_MAX_RETRIES`, `QUALITY_CHECKPOINT_DB_RETRY_BACKOFF_SECONDS`) for conversion stage, added stage remediation hints and stable exit code mapping (critical rendered/lighthouse/trend hard-fail with dedicated codes, non-critical conversion/alerts summarized unless `QUALITY_CHECKPOINT_STRICT_NON_CRITICAL=1`), and persisted execution policy/warnings metadata in checkpoint JSON for machine-readable diagnostics; added deterministic harness `scripts/tests/run_quality_checkpoint_test.sh` with stubbed stage runner coverage for retry-success, retry-exhaustion non-critical continue, strict non-critical failure, and critical-stage exit-code mapping; documented new controls in `docs/ops/QUALITY_CHECKPOINT_AUTOMATION.md`; verified with `bash -n scripts/run_quality_checkpoint.sh scripts/tests/run_quality_checkpoint_test.sh`, `npm run test:ops:checkpoint`, `npm run test:quality:snapshot`, `npm run test:conversion:insights`, `npm run test:ops:alerts`, `npm run lint`, `npm run test` (28 files, 137 tests), and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 7 mission-control card v2 fields for conversion reliability state (2026-03-18: extended `scripts/build_quality_ops_snapshot.ts` to ingest conversion connectivity/freshness metadata and emit mission-control conversion card fields `db_connectivity_status`, `conversion_pulse_mode`, `data_freshness_age`, and `amber_reason_code`; added snapshot-level conversion metadata (`mode`, `dbConnectivityStatus`, freshness age/stale, reason code, amber reason) and updated markdown highlights; hardened amber reason derivation for missing-stage, unavailable, stale-cache, stale-data, and warning-anomaly states while preserving red behavior for critical conversion states; expanded deterministic coverage in `scripts/tests/build_quality_ops_snapshot_test.sh` for missing conversion stage amber reason, stale-cached amber metadata propagation, degraded conversion-stage card generation, and connected-live red anomaly path; updated ops docs with card v2 field contract; verified with `npm run test:quality:snapshot`, `npm run test:ops:checkpoint`, `npm run test:conversion:insights`, `npm run test:ops:alerts`, `npm run lint`, `npm run test` (28 files, 137 tests), and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 7 conversion-pulse alert integration (prolonged amber + immediate red outage) (2026-03-18: upgraded `scripts/build_ops_alert_summary.ts` to ingest conversion pulse runtime state (`mode`, `status`, reason codes, freshness), add immediate critical alert `conversion_pulse_hard_outage` for hard DB connectivity outages, and track amber-state persistence with threshold window (`QUALITY_ALERT_AMBER_THRESHOLD_HOURS`, default `6`) before emitting warning alert `conversion_pulse_amber_prolonged`; introduced state migration to alert-state v2 with persistent condition tracking (`conditions`) to measure amber duration across runs, while preserving cooldown dedupe for non-critical repeats and immediate emission for critical alerts; extended alert summary/report with conversion pulse state diagnostics (mode, reason, amber elapsed/threshold); expanded deterministic coverage in `scripts/tests/ops_alert_tuning_test.sh` for hard-outage critical bypass and prolonged-amber threshold + cooldown suppression behavior; updated ops docs for new threshold control and alert semantics; verified with `npm run test:ops:alerts`, `npm run test:ops:checkpoint`, `npm run test:quality:snapshot`, `npm run test:conversion:insights`, `npm run lint`, `npm run test` (28 files, 137 tests), and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Wave 7 validation + reporting pack (2026-03-18: ran full validation matrix (`npm run test:ops:alerts`, `npm run test:ops:checkpoint`, `npm run test:quality:snapshot`, `npm run test:conversion:insights`, `npm run lint`, `npm run test`, env-backed `npm run build`, and `npm run ops:quality-checkpoint`), deployed latest commit to VPS (`./scripts/deploy_vps.sh --allow-dirty --skip-install --skip-migrate`, success), captured VPS runtime conversion evidence with connected DB mode (`connected_live`, `reasonCode=live_database`) from `/root/smartprintai`, generated Wave 7 artifact pack (`wave7-db-connectivity-*`, `wave7-conversion-pulse-*`, `wave7-quality-ops-card-v2-*`, `wave7-checkpoint-runs-*`) including before/after card comparison (`amber -> green` on conversion pulse), and published consolidated report `docs/reports/WAVE7_VALIDATION_2026-03-18.md`)
- [x] Wave 8 VPS Lighthouse runtime parity (P1) (2026-03-18: added deterministic browser resolver `scripts/resolve_lighthouse_chrome_path.sh`, wired checkpoint preflight/runtime metadata in `scripts/run_quality_checkpoint.sh` (explicit `QUALITY_CHECKPOINT_LIGHTHOUSE_RESOLVER` + `LIGHTHOUSE_CHROME_PATH` handoff), expanded deterministic shell coverage (`scripts/tests/resolve_lighthouse_chrome_path_test.sh`, updated `scripts/tests/run_quality_checkpoint_test.sh`), and documented runtime parity controls in `docs/ops/QUALITY_CHECKPOINT_AUTOMATION.md` + `docs/SEO_QUALITY_GATES.md`; verified locally with `bash -n scripts/run_quality_checkpoint.sh scripts/resolve_lighthouse_chrome_path.sh scripts/tests/run_quality_checkpoint_test.sh scripts/tests/resolve_lighthouse_chrome_path_test.sh`, `npm run test:ops:lighthouse-runtime`, `npm run test:ops:checkpoint`, `npm run lint`, `npm run test`, and env-backed `npm run build`; installed and pinned VPS runtime (`google-chrome-stable 146.0.7680.153`, `apt-mark hold`) and re-ran `/root/smartprintai/scripts/run_quality_checkpoint.sh` with Lighthouse stage passing on-host)
- [x] Wave 8 env loader standardization for VPS ops scripts (2026-03-18: added shared bootstrap helper `scripts/lib/env_loader.sh` to load `.env.local` (or `QUALITY_CHECKPOINT_ENV_FILE`/`SMARTPRINTAI_ENV_FILE`) with explicit required-variable checks and deterministic remediation messages, hardened parsing to ignore malformed non-`KEY=VALUE` lines with explicit warnings (no shell execution), wired `scripts/run_quality_checkpoint.sh` to enforce env bootstrap before stages (requiring `DATABASE_URL`), added contract tests `scripts/tests/env_loader_test.sh`, updated checkpoint harness fixtures (`scripts/tests/run_quality_checkpoint_test.sh`) to use the shared loader, and documented the env-loader contract in `docs/ops/QUALITY_CHECKPOINT_AUTOMATION.md`; verified with `bash -n scripts/lib/env_loader.sh scripts/run_quality_checkpoint.sh scripts/tests/env_loader_test.sh scripts/tests/run_quality_checkpoint_test.sh`, `npm run test:ops:env-loader`, `npm run test:ops:checkpoint`, `npm run lint`, `npm run test`, and env-backed `npm run build`)
- [x] Wave 8 artifact pull/sync automation with run mapping (2026-03-18: added one-command sync script `scripts/sync_vps_checkpoint_artifacts.sh` (`npm run ops:quality-checkpoint:sync`) to pull latest checkpoint-linked VPS artifacts into local `docs/reports` paths, emit Wave 8 sync summaries under `docs/reports/artifacts/wave8-artifact-sync-<timestamp>-<sha>/`, and preserve timestamp + commit SHA linkage from `latest.json`; added deterministic fixture harness `scripts/tests/sync_vps_checkpoint_artifacts_test.sh` (success + missing-source failure) and documented exact sync workflow/overrides in `docs/ops/QUALITY_CHECKPOINT_AUTOMATION.md`; verified with `bash -n scripts/sync_vps_checkpoint_artifacts.sh scripts/tests/sync_vps_checkpoint_artifacts_test.sh`, `npm run test:ops:artifact-sync`, `npm run test:ops:checkpoint`, `npm run lint`, `npm run test`, and env-backed `npm run build`)
- [x] Wave 8 validation + report closeout (2026-03-18: ran full local validation (`npm run test:ops:lighthouse-runtime`, `npm run test:ops:env-loader`, `npm run test:ops:artifact-sync`, `npm run test:ops:checkpoint`, `npm run lint`, `npm run test`, env-backed `npm run build`), deployed final Wave 8 runtime hardening to VPS (`./scripts/deploy_vps.sh --allow-dirty --skip-install --skip-migrate`, commit `43fe596`), executed VPS checkpoint rerun with Lighthouse pass and conversion pulse `connected_live`, synced artifacts via `npm run ops:quality-checkpoint:sync`, generated Wave 8 proof artifacts (`wave8-vps-lighthouse-parity-*`, `wave8-env-loader-check-*`, `wave8-artifact-sync-*`), and published consolidated report `docs/reports/WAVE8_VALIDATION_2026-03-18.md`)
- [x] `/create` generated-design white-matte cleanup for Gooten/Printful mockups (2026-03-19: added server-side image normalization in `/api/generate` to remove edge-connected white backgrounds and crop artwork bounds before upload, with safe fallback to original image on cleanup failure; added regression coverage in `src/lib/design-image.test.ts` and updated generate-route tests; verified with `npm run lint`, `npm run test -- src/lib/design-image.test.ts src/app/api/generate/route.test.ts`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] `/create` prompt quality guidance + transparent-output prompt hardening (2026-03-19: strengthened Gemini print prompt to require alpha transparency and no white frame/background (`buildPrintReadyPrompt`), added prompt-contract regression test `src/lib/gemini.test.ts`, and added localized on-page prompt guidance blocks/examples on `/create` for exact-output prompting (including mug/bottle-friendly constraints); verified with `npm run lint`, `npm run test -- src/lib/gemini.test.ts src/lib/design-image.test.ts src/app/api/generate/route.test.ts src/app/create-trust-visibility-regression.test.ts`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] `/create` detached checkerboard-matte cleanup for Gooten mockups (P1) (2026-03-19: hardened `src/lib/design-image.ts` background removal with a secondary transparent-neighbor seed path for matte islands (transparent outer frame + inner neutral checkerboard block), plus anchor-distinct safety gating to avoid stripping intentional white-on-transparent artwork; expanded regression coverage in `src/lib/design-image.test.ts` for checkerboard-matte removal and white-art preservation; verified with `npm run lint`, `npm run test -- src/lib/design-image.test.ts`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] `/create` Gooten drinkware AI preview quality gate (suppress wrap-template artifacts) (2026-03-19: deep-dived Gooten `productpreview` + `producttemplates` for mug/bottle SKUs and confirmed a single wrap-style print space per SKU with no alternate placement controls yielding cleaner preview; hardened `isMockupEligibleProduct` to block known problematic Gooten drinkware provider product IDs (`311`,`388`,`389`,`390`,`403`,`408`,`411`,`412`) from AI mockup flow, preventing strange rectangular wrap panels in customer `/create` previews while keeping stable apparel/cap SKUs available; added regression coverage in `src/lib/mockup-eligibility.test.ts`; verified with `npm run test -- src/lib/mockup-eligibility.test.ts`, `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] `/create` suppress Gooten stainless steel can holder AI preview artifact (`gooten:372`) (2026-03-20: triaged Antigravity QA report showing persistent white/gray wrap-panel behind transparent designs on `Stainless Steel Can Holders`, mapped failing SKU to provider product ID `372` from live `/api/products`, and extended `GOOTEN_MOCKUP_UNSUPPORTED_PROVIDER_PRODUCT_IDS` in `src/lib/mockup-eligibility.ts` to exclude this SKU from AI mockup flow; added regression coverage in `src/lib/mockup-eligibility.test.ts`; verified with `npm run test -- src/lib/mockup-eligibility.test.ts`, `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] `/create` `/api/mockup` 429 resilience for active browsing/generation loops (2026-03-20: moved route quota enforcement to cache-miss path in `src/app/api/mockup/route.ts` so already-generated previews return without consuming quota, and increased miss-path limit from `60/600s` to `180/600s` to prevent mobile flow lockout during normal regenerate/product-switch usage; added API regression coverage in `src/app/api/mockup/route.test.ts` for cache-hit bypass and cache-miss 429 behavior; verified with `npm run test -- src/app/api/mockup/route.test.ts`, `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] `/create` dark-matte black-box cleanup hardening for transparent mockups (P1) (2026-03-20: extended `src/lib/design-image.ts` background-removal detection to include dark neutral edge mattes and anchor-aware flood-fill behavior so black boxed backgrounds get stripped while preserving intentional artwork, added regression coverage in `src/lib/design-image.test.ts` (`removes full-frame dark matte behind centered subject`), and revalidated live Gooten hoodie/cap outputs with `npm run ops:mockup-quality:smoke` to confirm no suspicious matte components; verified with `npm run test -- src/lib/design-image.test.ts`, `npm run lint`, `npm run test`, `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`, and `npm run ops:mockup-quality:smoke`)
- [x] `/create` production throttle tuning for sustained session QA/customer exploration (2026-03-20: raised route defaults to reduce false-positive lockouts while preserving hard limits (`/api/generate` default `60/600s`, `/api/mockup` default `480/600s`), exposed both as env-configurable knobs in `.env.example` (`GENERATE_RATE_LIMIT*`, `MOCKUP_RATE_LIMIT*`), and added regression assertions in `src/app/api/generate/route.test.ts` and `src/app/api/mockup/route.test.ts` to prevent accidental rollback; verified with `npm run test -- src/app/api/generate/route.test.ts src/app/api/mockup/route.test.ts`, `npm run lint`, `npm run test`, `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`, live deploy `./scripts/deploy_vps.sh --allow-dirty --skip-install --skip-migrate`, and production cache-hit stress probe (`/api/mockup` 90/90 `200`, `429=0`))
- [x] `/create` full-frame checkerboard matte removal hardening (P1) (2026-03-19: expanded neutral-background cleanup thresholds in `src/lib/design-image.ts` to strip darker checkerboard matting that reaches image edges (the remaining square artifact source on apparel/cap mockups), and added a new regression in `src/lib/design-image.test.ts` covering a full-frame two-tone checkerboard behind the subject; verified with `npm run test -- src/lib/design-image.test.ts`, `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] `/create` production mockup-quality smoke guardrail (2026-03-19: added `src/lib/mockup-quality.ts` matte-artifact detector + regression coverage in `src/lib/mockup-quality.test.ts` for centered white-square detection versus valid dark-product artwork, and shipped `npm run ops:mockup-quality:smoke` (`scripts/mockup_quality_smoke.ts`) to run deterministic live checks against generated design + black Gooten hoodie/cap mockups with automatic provider retry handling; verified with `npm run test -- src/lib/mockup-quality.test.ts`, `npm run lint`, `npm run test`, `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`, and `npm run ops:mockup-quality:smoke`)
- [x] Checkpoint non-critical gate wiring for mockup-quality smoke artifacts (2026-03-19: integrated `mockup_quality` stage into `scripts/run_quality_checkpoint.sh` with artifact output (`wave9-mockup-quality-*` summary path), warning metadata, retention cleanup, and strict non-critical exit mapping (`43`); expanded checkpoint harness coverage in `scripts/tests/run_quality_checkpoint_test.sh` for pass/warn/strict-fail behavior; documented controls and outputs in `docs/ops/QUALITY_CHECKPOINT_AUTOMATION.md`; verified with `npm run test:ops:checkpoint`, `npm run lint`, `npm run test`, `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`, and `npm run ops:mockup-quality:smoke`)
- [x] CI non-critical mockup-quality gate wiring (2026-03-19: extended `scripts/ci_non_interactive.sh` to run `npm run ops:mockup-quality:smoke` with CI artifact output (`wave9-mockup-quality-ci-*`) before release guardrails, defaulting to warning-only behavior and adding opt-in strict fail mode via `CI_GUARD_STRICT_MOCKUP_SMOKE=1` (`CI_GUARD_MOCKUP_BASE_URL` override supported); updated `docs/SEO_QUALITY_GATES.md` CI integration notes; verified with `bash -n scripts/ci_non_interactive.sh`, `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] `/create` suppress unstable Gooten all-over hoodie templates from AI mockup flow (2026-03-19: extended Gooten AI-unsupported provider product IDs in `src/lib/mockup-eligibility.ts` to include `280/281/282` (all-over pullover/sweatshirt/zip-up hoodie templates that generate split/template-like previews for logo-style designs), and added regression coverage in `src/lib/mockup-eligibility.test.ts`; verified with `npm run test -- src/lib/mockup-eligibility.test.ts`, `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] `/create` product-specific prompt guidance by print-surface profile (2026-03-20: added deterministic guidance resolver `src/lib/create-product-guidance.ts` with profile-specific tips/examples for small-print accessories, all-over products, drinkware, and standard apparel; integrated contextual guidance panel in `src/components/create/CreatePageClient.tsx` so prompt instructions adapt to the selected product before mockup generation; added regression tests in `src/lib/create-product-guidance.test.ts`; verified with `npm run test -- src/lib/create-product-guidance.test.ts`, `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] `/create` guidance UI regression gate in CI quality suites (2026-03-20: added `src/app/create-guidance-regression.test.ts` to assert contextual guidance render anchors remain in `CreatePageClient` and profile coverage stays in `src/lib/create-product-guidance.ts`, and wired this test into `npm run test:seo:gates` in `package.json` so CI/guardrail runs fail on guidance removal drift; verified with `npm run test -- src/app/create-guidance-regression.test.ts`, `npm run test:seo:gates`, `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] VPS quality checkpoint critical SEO/UI gate wiring (2026-03-20: extended `scripts/run_quality_checkpoint.sh` to execute `npm run test:seo:gates` as a critical `seo_gates` stage (tracked in checkpoint JSON as `stages.seoGates.status`) with dedicated exit code `15`, added stage hint coverage, expanded checkpoint harness stubs/assertions in `scripts/tests/run_quality_checkpoint_test.sh` (including dedicated critical-exit test for `test:seo:gates`), and documented the new stage + exit code in `docs/ops/QUALITY_CHECKPOINT_AUTOMATION.md`; verified with `bash -n scripts/run_quality_checkpoint.sh scripts/tests/run_quality_checkpoint_test.sh`, `npm run test:ops:checkpoint`, `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)
- [x] Publish first 2 SEO blog posts (intent-focused) with live routes `/blog` and `/blog/[slug]`
- [x] Publish third multilingual SEO blog post (2026-03-11: `top-selling-ai-tshirt-themes-you-can-launch-this-week` in `en/fr/de/es`)
- [x] Publish SEO blog expansion batch (2026-03-13: added 6 new high-intent blog topics for fitness merch, family reunions, bachelorette events, Etsy mug niches, graduation gifts, and small-business merch; wired into all locale blog routes and sorted blog index by latest publish date)
- [x] Add SEO internal-linking layer for blog articles (2026-03-13: implemented keyword-aware related-article recommendations on default and localized blog post pages to increase crawl depth and topic authority)
- [x] Add final favicon/app icon pack and verify social preview image consistency (OG/Twitter/favicon/browser tab icons) (2026-03-11: Next file-based metadata routes added for `/icon`, `/apple-icon`, `/opengraph-image`, `/twitter-image`, and `/manifest.webmanifest`; validated via lint/typecheck/tests/build)
- [x] Add Careers page (`/careers`) and wire global discovery links (navbar, footer, sitemap) (2026-03-11)
- [x] Landing page theme consistency fix (2026-03-22: fixed homepage light/dark parity by removing hardcoded dark shell lock in `src/components/home/HomeLanding.tsx`, making premium-home tokens theme-aware for both `html[data-theme-mode='light']` and `html[data-theme-mode='dark']` in `src/app/globals.css`, and normalizing dark-locked utility classes (`text-zinc*`, `text-white`, `border-white/*`, dark hex backgrounds/gradients) inside `.premium-home-shell` to premium theme variables so hero/trust/product-proof/FAQ/footer surfaces now respond to theme toggles consistently; updated footer wordmark contrast class in `src/components/layout/Footer.tsx`; added regression guard in `src/app/home-brand-v2-regression.test.ts`; verified with `npm run lint`, `npm run test` (45 files, 225 tests), `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`, and local production-like screenshot check via `npx playwright screenshot http://127.0.0.1:3100/ /tmp/home-theme-dark.png`)
- [x] Create flow legacy accent cleanup to brand blue→teal system (2026-03-22: replaced remaining purple/pink primary accents across `/create` surfaces in `src/components/create/{PromptInput,StyleSelector,ProductPicker,GeneratedImage,MockupPreview,CreatePageClient}.tsx` and `src/components/shared/TrustSignalStrip.tsx` (Generate CTA, focus rings, selected states, helper links, loaders) with SmartPrintAI primary gradient `#2f6cf3 -> #26d4b8` and aligned brand focus treatment; verified with `npm run lint`, `npm run test`, and `DATABASE_URL=postgresql://user:pass@localhost:5432/db STRIPE_SECRET_KEY=sk_test_dummy GEMINI_API_KEY=dummy RESEND_API_KEY=dummy AUTH_SESSION_SECRET=0123456789abcdef NEXT_PUBLIC_APP_URL=https://smartprintai.com npm run build`)

### P2 - Marketing Execution (Month 1)
- [ ] Publish 5 TikToks per week
- [ ] Create Instagram Business account and publish 5 Reels per week (behind-the-scenes, prompt tips, product reveals)
- [ ] Create Facebook Page and connect it to Instagram for cross-posting + social proof
- [ ] Create Pinterest account and start daily product/mockup pins
- [ ] Create YouTube Shorts channel and publish 3 short-form videos per week
- [x] Maintain a weekly social content calendar across TikTok/Instagram/Pinterest/YouTube (2026-03-20: added reusable planner `docs/marketing/SOCIAL_CONTENT_CALENDAR_TEMPLATE.md` with per-channel weekly matrix, UTM naming standard, minimum posting targets, and weekly performance review section; linked from `docs/marketing/WEEKLY_CONTENT_BATCH_WORKFLOW.md` for batch-to-calendar execution)
- [x] Build reusable weekly content batch workflow (90 min/week) (2026-03-11: added generator script `npm run marketing:weekly-batch` + docs `docs/marketing/WEEKLY_CONTENT_BATCH_WORKFLOW.md` with 90-minute operating cadence and quality gate)

### P2 - Marketing Execution (Month 2)
- [ ] Launch first $100 Google Shopping test (prereq done 2026-03-11: product feed endpoint `/google/merchant-feed.xml`, setup runbook `docs/marketing/GOOGLE_MERCHANT_FEED_SETUP.md`; campaign launch still manual in Google Ads)
- [ ] Launch first Google Performance Max campaign using Merchant Center products (conversion objective + ROAS target)
- [ ] Launch branded Google Search campaign to protect brand queries and capture high-intent traffic
- [x] Add weekly Google Ads optimization checklist (search-term negatives, product-group cuts, bid/ROAS adjustments) (2026-03-20: added `docs/marketing/GOOGLE_ADS_WEEKLY_OPTIMIZATION_CHECKLIST.md` with weekly KPI intake, search-term negative workflow, product-group bid/ROAS decision rules, conversion integrity checks, and weekly change-log template for Shopping + Performance Max + branded Search operations)
- [x] Add owner manual execution-order checklist for remaining UI-only launch tasks (2026-03-20: added `docs/marketing/OWNER_MANUAL_EXECUTION_ORDER.md` with today/tomorrow sequence covering live purchase proof capture, Google Shopping/PMax/Branded campaign launch order, social-account rollout minimums, and exact artifacts required for Codex TODO closure)
- [x] Set up email list capture offer (first-order discount popup) (2026-03-11: homepage discount modal in `en/fr/de/es`, lead capture API `/api/marketing/lead`, rate-limited submission, internal lead notification + customer coupon email via Resend, env vars documented)
- [x] Prepare nano-influencer outreach kit (2026-03-20: added `docs/marketing/NANO_INFLUENCER_OUTREACH_KIT.md` with qualification criteria, outreach/follow-up templates, collaboration brief, KPI targets, and weekly review checklist; added tracker seed `docs/marketing/NANO_INFLUENCER_TRACKER_TEMPLATE.csv` for lead pipeline and conversion tracking)
- [ ] Outreach to 5 nano-influencers for product exchange
- [x] Set up Make.com automations (completed 2026-03-12: all 5 flows validated from VPS with `200 Accepted` via shared fast-route webhook on free Make plan; secured daily digest route verified `200`; runbook `docs/marketing/MAKE_AUTOMATIONS_SETUP.md`; optional future upgrade: split into dedicated always-on scenarios on paid plan)
- [x] Assess Etsy integration path (catalog publishing + order sync + inventory/price source of truth) (2026-03-11: integration architecture and rollout documented in `docs/marketing/ETSY_INTEGRATION_PLAN.md` with source-of-truth model, data mappings, phased implementation, risk controls, and success metrics)
- [x] Prepare Etsy-ready listing pack (titles, tags, thumbnails, mockups) for top 20 designs (2026-03-11: exported `docs/marketing/etsy/ETSY_TOP20_LISTING_PACK.csv` with 20 listing-ready entries including Etsy title, 13 tags, prompt, product mapping, thumbnail/mockup briefs, and starting price)

### P2 - International Growth
- [x] Implement SEO-safe i18n architecture with locale routes and hreflang (`en` default + `fr`, `de`, `es` phase 1) (2026-03-11: localized routes live for `/[locale]` and `/[locale]/careers`, translated copy foundation in `src/lib/i18n.ts`, language switchers, localized sitemap entries)
- [x] Localize core conversion pages (home, create, products, product detail, cart, checkout-success, support, careers) (completed 2026-03-11: locale routes + translated UI copy for all listed pages in `en/fr/de/es`)
- [x] Expand multilingual SEO content (blog posts per priority locale) with localized metadata and internal links (2026-03-11: blog index + article routes localized for `en/fr/de/es`, translated article content shipped for 2 core posts, hreflang/canonical metadata and localized internal links to `/create` + `/products`, localized blog URLs added to sitemap)

### P3 - Scale Triggers (After first consistent revenue)
- [ ] Upgrade image generation quality tier (fal.ai phase) when threshold is reached
- [ ] Increase Google Shopping budget to next step based on ROAS
- [ ] Run first boosted TikTok test campaign

## Execution Rule
- [ ] Execute one item at a time from top to bottom.
- [ ] After each item: run checks, record result, then move to next item.
